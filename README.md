# 🐍 CodeViper

**CodeViper** — локальный AI-агент с GUI для программирования. Работает полностью на вашем компьютере через [Ollama](https://ollama.com). Никаких подписок, API-ключей и облачных лимитов.

![stack](https://img.shields.io/badge/Electron-36-47848F) ![stack](https://img.shields.io/badge/React-19-61DAFB) ![stack](https://img.shields.io/badge/Ollama-local-7EE787)

## Roadmap

- [x] Стриминг ответов в реальном времени
- [x] Самообучение и память агента
- [x] История чатов
- [ ] Поддержка OpenAI-compatible API (LM Studio)
- [ ] Тёмная/светлая тема

## Возможности

- 💬 **Чат-агент** — ставит задачи, планирует, отвечает на русском; ответы приходят **потоком** в реальном времени
- 📚 **История чатов** — папки, сохранение переписки, переключение между задачами
- 📁 **Файлы** — агент читает и редактирует файлы внутри открытого проекта
- 🖥️ **Терминал** — запускает команды в корне проекта
- 🔒 **Приватность** — код не уходит в облако
- 🧠 **Модели Ollama** — выбор установленных и скачивание рекомендуемых прямо из приложения
- 🧬 **Самообучение** — агент запоминает уроки, предпочтения и правила проекта между сессиями

## Что нужно

| Компонент | Минимум |
|---|---|
| Windows 10/11 | ✅ |
| RAM | 8 GB (16 GB лучше) |
| Node.js | 18+ |
| Ollama | [ollama.com](https://ollama.com) |
| Модель | `qwen2.5-coder:7b` или новее |

## Быстрый старт

### 1. Установите Ollama

Скачайте и установите [Ollama](https://ollama.com). После установки приложение Ollama должно быть запущено (иконка в трее).

Модели можно скачать **прямо в CodeViper** — в панели «Настройки» справа есть список рекомендуемых моделей и кнопка «Скачать».

Рекомендуемые модели с поддержкой tool calling:
- `qwen2.5-coder:7b` — быстро, 8 GB RAM
- `qwen2.5-coder:14b` — лучше качество, 16 GB RAM
- `llama3.1:8b` — универсальная с tool calling

### 2. Запуск

**Один раз** после клонирования:

```powershell
cd CodeViper
npm install
```

Дальше — **двойной клик**, без терминала каждый раз:

| Файл | Что это | Когда |
|---|---|---|
| **`CodeViper.exe`** | Готовое окно приложения (portable) | Повседневная работа |
| **`CodeViper.cmd`** | Запуск из исходников **без консоли** (скрытый) | Повседневная работа из папки репо |

#### CodeViper.exe (рекомендуется)

Первый раз собери exe (нужен Node.js):

```powershell
.\rebuild-exe.bat
# или: npm run app:exe
```

Появится **`CodeViper.exe`** в корне — запускай двойным кликом. Ollama должна быть в трее.

После изменений кода снова запусти `rebuild-exe.bat`.

#### CodeViper.cmd (без сборки, без консоли)

Двойной клик по **`CodeViper.cmd`** — скрытый запуск `npm run dev` (окно терминала не показывается).

Для отладки с консолью: `CodeViper.cmd console`  
Лог: `%LOCALAPPDATA%\CodeViper\launch.log`

Ollama должна быть установлена и запущена отдельно — CodeViper подключается к ней по `http://127.0.0.1:11434`.

### 3. Использование

1. Нажмите **«Открыть проект»** и выберите папку с кодом, **над которым работаете**
2. Слева создайте **новый чат** или выберите из истории (можно группировать в **папки**)
3. Убедитесь, что индикатор Ollama **зелёный**
4. Скачайте модель (если ещё нет) или выберите установленную в настройках справа
5. Опишите задачу в чате — агент сам прочитает файлы, внесёт правки и запустит команды

**Два разных «проекта»:**
- **CodeViper** — папка, откуда вы запускаете `npm run dev` (само приложение)
- **Рабочий проект** — папка, которую выбираете кнопкой «Открыть проект»; агент работает только внутри неё

## История чатов

- Слева — список чатов и **папки** для группировки
- Каждый чат привязан к проекту и **сохраняется автоматически** (debounce 500 ms + **немедленный flush** при смене чата)
- Хранилище: `%APPDATA%/CodeViper/chats.json`
- Фильтр «Все чаты» / только текущий проект
- Переименование, удаление, перенос чата между папками
- **Стрим агента привязан к chatId** — события попадают только в тот чат, где запущена задача
- **Один активный run-agent** — параллельные запросы блокируются до завершения текущего

## Самообучение

Агент накапливает опыт локально и использует его в следующих задачах:

| Что | Где хранится |
|---|---|
| Глобальные знания (паттерны, навыки) | `%APPDATA%/CodeViper/memory.json` |
| Знания проекта | `{проект}/.codeviper/memory.json` |
| Правила проекта | `{проект}/.codeviper/rules.md` |
| **Навыки (skills)** | `%APPDATA%/CodeViper/skills.json` и `{проект}/.codeviper/skills.json` |
| **Данные навыков** (todo, состояние) | `%APPDATA%/CodeViper/skill-data/` и `{проект}/.codeviper/skill-data/` |

**Как это работает:**
1. Перед задачей агент подгружает релевантные знания и **активные skills** в системный промпт
2. Во время работы: `remember`, `search_memory`, `forget`
3. **По запросу** («сделай skill для todo», «улучши себя»): `create_skill`, `update_skill`, `read_skill`, `read_skill_data`, `write_skill_data`
4. После задачи с инструментами — автоматическая рефлексия (если включено «Самообучение»)

**Примеры запросов:**
```
Сделай себе skill для todo-листа в этом проекте
```
```
Улучши себя: добавь skill для code review перед коммитом
```

Память и навыки можно просмотреть в «Настройки» справа.

## Примеры задач

```
Добавь валидацию email в форму регистрации
```

```
Найди все TODO в проекте и выведи список
```

```
Запусти тесты и исправь упавшие
```

## Архитектура

```
CodeViper/
├── electron/
│   ├── main/          # Main process: агент, Ollama, файлы, терминал
│   └── preload/       # Безопасный мост IPC
└── src/               # React GUI
    └── components/    # Чат, история, терминал
```

Агент использует цикл **ReAct**:
1. Отправляет запрос в Ollama с инструментами, памятью, **путём проекта**, **структурой файлов** и **полной историей чата**
2. Модель вызывает `read_file`, `write_file`, `list_directory`, `run_command`, `remember`, `search_memory`
3. Результаты возвращаются модели
4. Повторяется до завершения задачи (макс. 12 шагов по умолчанию)
5. После задачи — рефлексия и сохранение уроков (если самообучение включено)

## Безопасность

- Агент работает **только внутри** выбранной папки проекта
- Файлы больше 500 KB не читаются
- Опасные команды (`rm -rf`, `format`, `shutdown`) заблокированы
- Команды в терминале автоматически прерываются через **120 секунд** (exit code 124)
- Все данные остаются на вашем ПК

## Разработка

```powershell
npm run dev        # GUI + hot reload (или двойной клик CodeViper.cmd)
npm run build      # сборка в out/
npm start          # запуск собранной версии
npm run typecheck  # проверка TypeScript
npm run app:exe    # собрать CodeViper.exe (portable)
```

## Сравнение с Cursor

| | Cursor Pro | CodeViper |
|---|---|---|
| Цена | $20/мес | $0 |
| Лимиты | Месячные пулы | Нет (железо) |
| Качество AI | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ (зависит от модели) |
| Приватность | Облако | 100% локально |
| Tab autocomplete | ✅ | ❌ (пока) |
| Agent | ✅ | ✅ |

**Идеальный сценарий:** Cursor для Tab и сложных задач, CodeViper — когда лимит Cursor на 100%.

## Лицензия

MIT
