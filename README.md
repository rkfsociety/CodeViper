<p align="center">
  <img src="app/resources/icon.png" alt="CodeViper" width="512" />
</p>

# CodeViper

**CodeViper** — локальный AI-агент с GUI для программирования. Работает полностью на вашем компьютере через [Ollama](https://ollama.com). Никаких подписок, API-ключей и облачных лимитов.

Встроенного редактора кода нет: вы описываете задачу в чате, агент сам читает файлы, правит их и запускает команды в терминале.

![stack](https://img.shields.io/badge/Electron-36-47848F) ![stack](https://img.shields.io/badge/React-19-61DAFB) ![stack](https://img.shields.io/badge/Ollama-local-7EE787)

## Roadmap и TODO

### ✅ Сделано

- [x] Рефакторинг: общий `makeId` / `readNdjsonLines`, удалён мёртвый `FileTree.tsx`
- [x] Хранилище чатов v2: атомарная запись, лимиты, отдельный файл на чат (`chats/index.json` + `chats/data/{id}.json`)
- [x] Иконка приложения (`app/resources/icon.png`)
- [x] **Enter** — отправка, **Shift+Enter** — новая строка; очередь сообщений пока модель отвечает
- [x] **Инструменты файлов:** `create_file`, `edit_file`, `append_file`, `delete_file`, `move_file` (+ аналоги для CodeViper)
- [x] **Автономное самоулучшение:** план `set_self_improvement_plan` + цикл до выполнения всех пунктов
- [x] **Поиск в коде:** `grep_files`, `find_files` (+ CodeViper); **9 встроенных skills** (agent-core, viper-skills, files, codebase…)
- [x] **Каталог моделей Ollama** — только модели с tool calling, группировка по RAM (6 GB → 48 GB+); удаление установленных
- [x] **Автовыбор модели** — под задачу, выгрузка других моделей из RAM Ollama
- [x] **Проверка зависимостей** перед запуском агента (Ollama, модель, npm install)
- [x] **История:** поиск, drag-and-drop чатов в папки, модалки вместо `prompt`/`confirm`
- [x] Проект выбирается **внутри чата**, у каждого чата свой проект
- [x] Стриминг ответов в реальном времени
- [x] Самообучение и память агента (`remember`, рефлексия после задач)
- [x] История чатов (папки, автосохранение)
- [x] **Просмотр контекста модели** — панель «Контекст»: % лимита, секции, ~токены; суммаризация при ~85%+
- [x] Скачивание моделей Ollama из приложения
- [x] Скрытие сырого JSON tool call в чате; парсинг и выполнение, если модель пишет `{"name": ...}` текстом
- [x] Проверка «галлюцинаций»: повтор с `tool_choice: required` при советах вместо tool call (Figma, пошаговые планы), затем честная ошибка
- [x] Стрим привязан к `chatId`, блокировка параллельных запусков
- [x] Flush чата при переключении (без потери сообщений)
- [x] Путь проекта и дерево файлов в системном промпте
- [x] Кнопка **«Стоп»** + отмена fetch к Ollama
- [x] **Суммаризация контекста** — при ~85%+ лимита модели старая история сжимается через Ollama (не просто обрезка)
- [x] Позиция и размер окна сохраняются между запусками (`window-state.json`)
- [x] Безопасность `run_command` (blocklist, без `shell: true`)
- [x] Настройки в модальном окне, терминал по кнопке
- [x] Запуск через `CodeViper.cmd` в корне репозитория
- [x] Unit-тесты (`vitest`): `chats`, `memory`, `services`, `parseToolArgs`, а также `shared/` (`modelRouter`, `toolCalls`, `fileEdit`, `actionVerification`, `contextLimits`, `skillMatching`)
- [x] CI на GitHub Actions: `typecheck` → `test` → `build`
- [x] Устойчивость хранилищ: повреждённые `ViperMemory.md` / индекс чатов / файлы чатов сохраняются как `*.corrupt-<ts>` вместо молчаливой перезаписи пустыми
- [x] Таймауты для запросов к Ollama (ping/список моделей/удаление) — нет зависаний при недоступной Ollama
- [x] **Режимы доступа** — `Спрашивать всё` / `Принимать правки, спрашивать команды` / `Без подтверждений` (bypass)
- [x] **Режим уточняющих вопросов** — при неоднозначной задаче агент сначала задаёт вопросы, потом действует

## Возможности

- 💬 **Чат-агент** — ставит задачи, планирует, отвечает на русском; ответы приходят **потоком** с **Markdown**-разметкой; кнопка **«Копировать»** у каждого сообщения
- 📚 **История чатов** — папки, сохранение переписки, переключение между задачами
- 📁 **Файлы** — агент читает, **создаёт** (`create_file`), **редактирует точечно** (`edit_file`), перезаписывает (`write_file`) и дописывает (`append_file`) файлы в проекте
- 🖥️ **Терминал** — запускает команды в корне проекта
- 🔒 **Приватность** — код не уходит в облако
- 🧠 **Модели Ollama** — выбор установленных и скачивание рекомендуемых прямо из приложения
- 🧬 **Самообучение** — агент запоминает уроки, предпочтения и правила проекта между сессиями
- 🎨 **Интерфейс** — карточки шаблонов задач, быстрые подсказки, бейджи ролей в чате, статус модели/Ollama в шапке

## Что нужно

| Компонент | Минимум |
|---|---|
| Windows 10/11 | ✅ |
| RAM | 8 GB (16 GB лучше) |
| Node.js | 18+ |
| Ollama | [ollama.com](https://ollama.com) |
| Модель | `qwen2.5-coder:7b` или новее |

## Быстрый старт

### 1. Установите Ollama

Скачайте и установите [Ollama](https://ollama.com). После установки приложение Ollama должно быть запущено (иконка в трее).

Перед запуском агента CodeViper проверяет **Ollama**, наличие **модели с tool calling** и **npm-зависимости** проекта (`package.json` без `node_modules`). Если чего-то не хватает — баннер с кнопками: установить зависимости, скачать модель, проверить снова.

Модели можно скачать **только из каталога** в CodeViper — кнопка **«Настройки»** в верхней панели. Уже установленные **не показываются** в каталоге. Несколько моделей — кнопка **«В очередь»**, скачиваются **по порядку**; окно настроек можно закрыть, прогресс — в верхней панели.

| RAM | Рекомендуем | Альтернативы |
|---|---|---|
| **6–8 GB** | `qwen2.5-coder:3b` ★ | `llama3.2:3b`, `qwen3:4b` |
| **8 GB** | `qwen2.5-coder:7b` ★ | `qwen3:8b`, `llama3.1:8b`, `qwen2.5:7b` |
| **12 GB** | `mistral-nemo:12b` | — |
| **16 GB** | `qwen2.5-coder:14b` ★ | `qwen3:14b`, `qwen2.5:14b`, `codestral:22b` |
| **24–32 GB** | `qwen2.5-coder:32b` ★ | `qwen3:30b` |
| **48 GB+** | `llama3.3:70b` ★ | `llama3.1:70b`, `qwen2.5:72b` |

★ — отмечены в приложении как лучший выбор в категории.

Не подходят для агента (нет tool calling): `llama2`, `gemma2`, `deepseek-r1`, старый `mistral:7b` — такие модели не попадают в каталог; если уже установлены в Ollama, показываются отдельно с предложением удалить.

### Автовыбор модели (режим Auto)

По умолчанию включён **автовыбор модели**. Если установлено **несколько** моделей, перед каждым запросом агент:

1. Оценивает сложность задачи (короткий вопрос → лёгкая модель, самоулучшение/refactor → тяжёлая)
2. Выбирает лучшую **установленную** модель (предпочитает coder/reasoning)
3. **Выгружает** другие модели из RAM Ollama (`keep_alive: 0`)
4. Пишет в чат: «🤖 Авто-модель: …» с причиной выбора

В шапке отображается **Auto · qwen2.5-coder**. Отключить: **Настройки → снять «Автовыбор модели»** — тогда всегда используется выбранная в списке модель.

### 2. Запуск

**Один раз** после клонирования (или просто первый двойной клик — скрипт сам сделает `npm install`):

```powershell
cd app
npm install
```

**Повседневный запуск** — двойной клик по **`CodeViper.cmd`** в **корне репозитория** (рядом с папкой `app/`). Консоль не показывается; при ошибке появится окно с подсказкой.

- Отладка с консолью: `CodeViper.cmd console`
- Лог launcher: `%LOCALAPPDATA%\CodeViper\launch.log`
- Лог `npm run dev`: `%LOCALAPPDATA%\CodeViper\dev.log`

Если двойной клик «ничего не делает» — запустите `CodeViper.cmd console` и смотрите вывод в терминале.

Ollama должна быть установлена и запущена отдельно — CodeViper подключается к ней по `http://127.0.0.1:11434`.

### 3. Использование

1. Слева нажми **«+ Чат»** — создай новый чат
2. В чате нажми **«Выбрать проект»** и укажи папку с кодом
3. Убедись, что индикатор Ollama **зелёный**
4. Скачай модель (если ещё нет) или выбери установленную в **Настройках**
5. Опиши задачу — агент сам прочитает файлы, внесёт правки и запустит команды

**Два разных «проекта»:**
- **CodeViper** — папка `app/` (исходники приложения)
- **Рабочий проект** — папка, которую выбираешь **внутри чата**; у каждого чата свой проект. После **первого сообщения** сменить проект нельзя

## История чатов

- Слева — список чатов и **папки** для группировки
- **Поиск** по названию чата и проекту
- **Drag-and-drop** — перетащи чат на папку или в «Без папки»
- Переименование и удаление через **модальные окна** (не `prompt`/`confirm`)
- Каждый чат привязан к **своему проекту** и **сохраняется автоматически** (debounce 500 ms + flush при смене чата)
- Данные чатов: `%APPDATA%/CodeViper/chats/index.json` (метаданные) и `chats/data/{id}.json` (сообщения); старый `chats.json` мигрируется автоматически
- **Стрим агента привязан к chatId** — события попадают только в тот чат, где запущена задача
- **Один активный run-agent** — параллельные запросы блокируются до завершения текущего
- Кнопка **«Стоп»** прерывает текущий запрос агента (отмена fetch к Ollama) и очищает очередь
- Пока модель отвечает, можно **продолжать писать** — новые сообщения попадают в **очередь** и обрабатываются по порядку
- Длинная история: при **~85%+** лимита контекста модели агент **суммаризирует** старые сообщения через Ollama; последние сообщения сохраняются. В панели «Контекст» — процент загрузки.

## Настройки

- Кнопка **«Настройки»** в верхней панели — модель, Ollama URL, самообучение, навыки
- Настройки **сохраняются между сессиями**: `%APPDATA%/CodeViper/settings.json`
  (модель, URL Ollama, лимит шагов, флаг самообучения)
- Позиция и размер окна запоминаются в `%APPDATA%/CodeViper/window-state.json`
- Навыки агента — в **Настройках** (глобальные, `%APPDATA%/CodeViper/ViperSkills.md`); память — для текущего чата

## Самообучение

Агент накапливает опыт локально и использует его в следующих задачах:

| Что | Где хранится |
|---|---|
| Глобальные знания (паттерны, навыки) | `%APPDATA%/CodeViper/ViperMemory.md` |
| Знания проекта | `{проект}/.codeviper/ViperMemory.md` |
| Правила проекта | `{проект}/.codeviper/rules.md` |
| **Навыки (skills)** | `%APPDATA%/CodeViper/ViperSkills.md` — **только глобальные** (поведение агента, не проекта) |
| **Viper Skills** (встроенный skill) | `viper-skills` — как создавать и применять навыки |
| **Viper Memory** (встроенный skill) | `viper-memory` — remember / search_memory / forget → **ViperMemory.md** |
| **Viper Model Training** (skill) | `viper-model-training` — адаптация Ollama через Modelfile + few-shot |
| **Viper Agent Core** | `viper-agent-core` — полный workflow и список инструментов |
| **Viper Files** | `viper-files` — read/create/edit/append/write файлов |
| **Viper Codebase** | `viper-codebase` — list_directory, grep_files, find_files |
| **Viper Terminal** | `viper-terminal` — run_command |
| **Viper Self-Edit** | `viper-self-edit` — правка исходников CodeViper |
| **Viper Self-Improvement** | `viper-self-improvement` — план автономного самоулучшения |
| **Данные навыков** (todo, состояние) | `%APPDATA%/CodeViper/skill-data/` |

**Как это работает:**
1. Перед задачей агент подгружает релевантные знания и **навыки агента** в системный промпт
2. Если запрос совпадает с **триггерами** пользовательского навыка — его **полные инструкции** подставляются автоматически
3. Во время работы: `remember`, `search_memory`, `forget`
4. **По запросу** («сделай skill для todo», «улучши себя»): `create_skill` → ViperSkills.md, `update_skill`, `read_skill`
5. После задачи **с изменениями** (запись файлов, команды и т.п.) — автоматическая рефлексия (если включено «Самообучение»); после чисто исследовательских задач не запускается

**Примеры запросов:**
```
Сделай себе skill для todo-листа в этом проекте
```
```
Улучши себя: добавь skill для code review перед коммитом
```

Память и навыки можно просмотреть в **Настройках** (кнопка в верхней панели). **Встроенные** навыки (`viper-*`) помечены «системный» — удалить их нельзя; пользовательские — кнопка ✕.

### Саморедактирование CodeViper

Агент может улучшать **свой** код и навыки (не только проект в чате):

| Действие | Инструмент |
|---|---|
| Навык (инструкции) | `create_skill` — всегда глобальный → ViperSkills.md |
| Новый файл в app/ | `create_codeviper_file` |
| Точечная правка | `edit_codeviper_file` (old_string → new_string) |
| Полная перезапись | `write_codeviper_file` |
| Дописать в конец | `append_codeviper_file` |
| Читать исходники | `read_codeviper_file` |
| Тесты после правок | `run_codeviper_command` → `npm run typecheck`, `npm test` |
| Структура приложения | `list_codeviper_directory` |
| Поиск в коде | `grep_codeviper_files`, `find_codeviper_files` |

Примеры:
```
Изучи код и начни улучшать себя
```

Агент войдёт в **режим автономного самоулучшения**:
1. Изучит исходники CodeViper (`list_codeviper_directory`, `read_codeviper_file`)
2. Создаст план через `set_self_improvement_plan` (3–8 пунктов)
3. Будет выполнять пункты **до конца** — после каждого `complete_self_improvement_item`
4. Остановится только когда все пункты done или достигнут лимит шагов (мин. 20)

После изучения кода текстовый ответ без tool call **не прерывает** сессию — агент получит nudge создать план через `set_self_improvement_plan` и продолжит работу. Если модель пишет план JSON текстом, CodeViper **примет его автоматически**; `tool_response {...}` в тексте выполняется как встроенный tool call.

Прогресс плана отображается системными сообщениями в чате.

```
Улучши себя: добавь skill для code review
```
```
Добавь в agent.ts поддержку …
```

После правок `electron/main/*` нужен **перезапуск** приложения.

### Адаптация моделей Ollama (Viper Model Training)

CodeViper **не выполняет классический GPU fine-tuning**. Вместо этого агент может создать **производную модель Ollama** из файла примеров:

1. Подготовьте JSON/JSONL: `[{"user":"…","assistant":"…"}]` → `.codeviper/training/examples.json`
2. В чате: «обучи модель на examples.json» или «создай модель my-coder из qwen2.5-coder:7b»
3. Агент вызовет `preview_ollama_modelfile`, затем `create_ollama_model`
4. Выберите новую модель в **Настройках**

Инструменты: `preview_ollama_modelfile`, `create_ollama_model`. Встроенный skill: `viper-model-training`.

## Примеры задач

```
Добавь валидацию email в форму регистрации
```

```
Найди все TODO в проекте и выведи список
```

```
Запусти тесты и исправь упавшие
```

## Архитектура

```
CodeViper/
├── README.md
├── CodeViper.cmd      # запуск приложения
└── app/               # исходники
    ├── resources/     # иконка приложения
    ├── shared/        # общие утилиты (makeId)
    ├── electron/      # Main process: агент, Ollama, файлы, терминал
    └── src/           # React GUI
```

Агент использует цикл **ReAct**:
1. Отправляет запрос в Ollama с инструментами, памятью, **путём проекта**, **структурой файлов** и **полной историей чата**
2. Модель вызывает `list_directory`, `grep_files`, `find_files`, `read_file`, `create_file`, `edit_file`, `write_file`, `run_command`, …

### Просмотр контекста (как в Cursor)

Над чатом — панель **«Контекст»** с **процентом лимита** (~токены, символы) и чипами секций. При **≥85%** подсветка предупреждения; при суммаризации — бейдж «Σ сводка». Клик открывает модальное окно:

| Секция | Что внутри |
|---|---|
| Инструкции агента | Базовый system prompt |
| Саморедактирование | Пути и workflow CodeViper |
| Проект | Корень и дерево файлов |
| ViperMemory и навыки | Релевантные записи и skills |
| Инструменты | Список function tools для Ollama |
| Сообщения | System + история + текущий запрос |

Контекст обновляется при наборе сообщения (черновик) и фиксируется при отправке. При запуске агента, если лимит близок к исчерпанию, старая история **автоматически суммаризируется** (отдельный вызов Ollama); в длинных задачах с множеством tool call — повторно перед следующими шагами.

### Почему модель «советует», но не делает сама

CodeViper **не выполняет** ваш запрос отдельно от модели — агент только передаёт текст в Ollama и **запускает инструменты, если модель их вызвала**. Частые причины «плана вместо действия»:

| Причина | Что делать |
|---|---|
| Модель без tool calling (llama2, mistral 7b и т.п.) | В настройках: `qwen2.5-coder:7b` или `llama3.1:8b` |
| Модель отвечает как консультант (Figma, Material-UI, «сделайте сами») | Переформулируйте: «**Прочитай** `styles.css` и **измени** …»; агент повторит запрос с обязательным tool call |
| Правка **самого CodeViper**, а открыт другой проект | Скажите явно: «используй `write_codeviper_file` для UI CodeViper» |
| Обрыв JSON в ответе (`}` в конце) | Слабая модель пыталась написать tool call текстом — смените модель |

В чате при повторе появится: *«Модель ответила текстом без инструментов — повторяю…»*. Если после повтора инструментов нет — смените модель или уточните задачу.

3. Результаты возвращаются модели
4. Повторяется до завершения задачи (макс. 12 шагов по умолчанию)
5. После задачи **с изменениями** — рефлексия и сохранение уроков (если самообучение включено)

## Безопасность

- Агент работает **только внутри** выбранной папки проекта
- **Режимы доступа** (в настройках) определяют, что агент делает без спроса:
  - `Спрашивать всё` — подтверждение перед каждым мутирующим инструментом (запись/создание/правка файлов, `run_command`, память/навыки, саморедактирование)
  - `Принимать правки, спрашивать команды` — правки файлов выполняются сразу, а команды и создание моделей требуют подтверждения
  - `Без подтверждений` (по умолчанию) — агент действует самостоятельно
  - Отказ в диалоге возвращается модели, и она продолжает без этого действия
- Файлы больше 500 KB не читаются
- Опасные команды блокируются (rm -rf, format, shutdown, diskpart, encoded powershell и др.)
- `run_command` запускается через `cmd.exe` / `sh` **без** `shell: true`; лимит длины команды — 4096 символов
- Команды в терминале автоматически прерываются через **120 секунд** (exit code 124)
- Повреждённые файлы хранилищ (память, индекс/данные чатов) не затираются — сохраняются как `*.corrupt-<ts>` для восстановления
- Все данные остаются на вашем ПК

> ⚠️ Блок-лист команд — **грубый фильтр**, а не песочница: его можно обойти (например, через интерпретатор `node -e`/`python -c` или запись и запуск скрипта). Агент имеет широкий доступ к файлам проекта, может выполнять команды и **редактировать собственные исходники**. Для контроля включите «Подтверждение действий» и запускайте агента только на доверенных проектах.

## Разработка

```powershell
cd app
npm run dev        # GUI + hot reload (или двойной клик CodeViper.cmd)
npm run build      # сборка в out/
npm start          # запуск собранной версии
npm run typecheck  # проверка TypeScript
npm test           # unit-тесты (vitest)
npm run test:watch # тесты в watch-режиме
```

## Сравнение с Cursor

| | Cursor Pro | CodeViper |
|---|---|---|
| Цена | $20/мес | $0 |
| Лимиты | Месячные пулы | Нет (железо) |
| Качество AI | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ (зависит от модели) |
| Приватность | Облако | 100% локально |
| Редактор + autocomplete | ✅ | ❌ (не нужен — всё через агента) |
| Agent | ✅ | ✅ |

**Идея CodeViper:** не IDE с подсказками, а чат с агентом, который сам правит проект. Cursor можно держать параллельно для ручного редактирования; CodeViper — когда нужен локальный агент без лимитов.

## Лицензия

MIT
