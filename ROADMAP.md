# Дорожная карта CodeViper

Планы развития и список выполненного. Назад в [README](README.md).

## 📋 В планах

Задачи отсортированы по сложности выполнения агентом: от однофайловых правок до отдельных инфраструктурных подсистем. Нумерация сквозная, начинается с 1.

---

### 🟢 Простые — 1 файл, 1–2 часа

- [ ] **1. Батчинг токенов 80 мс → 150 мс** — в `useAgentStream` заменить `setTimeout(flushPending, 80)` на 150; снижение CPU при Markdown-рендере длинных ответов на ~30–40%
- [ ] **2. Дебаунс контекст-превью 600 мс → 1000 мс** — изменить `CONTEXT_PREVIEW_DEBOUNCE_MS` в `shared/constants.ts`; меньше IPC-запросов при быстром наборе текста
- [ ] **3. React.memo на MessageBody** — обернуть `export function MessageBody` в `memo()`; `MessageRow` уже в `memo`; устраняет повторный Markdown-рендер при изменении других сообщений
- [ ] **4. Снизить частоту опроса системных статов до 3 с** — интервал сбора CPU/GPU в main-процессе с 1 с до 3 с; для индикатора достаточно
- [ ] **5. Исключить thinking из истории** — при построении контекста в `agentContext.ts` не включать поле `thinking` из `ChatMessage`; опция в настройках (вкладка Модель); экономия 20–50% для think-моделей
- [ ] **6. Очистка editSnapshots при старте прогона** — в `agentHandlersProject.ts` сбрасывать Map `editSnapshots` при каждом новом `runId`; предотвращает утечку памяти в длинных сессиях
- [ ] **7. Фикс гонки инициализации в embeddingQueue.ts** — запросы, пришедшие до `online`-события воркера, класть в очередь и отправлять сразу после готовности listener'а; сейчас они теряются

---

### 🟡 Средние — несколько файлов, полдня–день

- [ ] **8. PR-панель: опрос только при открытой панели** — `PrStatusPanel` принимает `isOpen`; `setInterval` запускается только при `isOpen === true`; интервал с 60 с до 300 с
- [ ] **9. Кэш контекст-превью по хешу** — в `useContextPreview` хранить последний результат для пары `{messagesLength, model}`; пропускать IPC-запрос, если вход не изменился
- [ ] **10. AbortSignal-таймаут в diffUtil.ts** — передавать `AbortSignal` в `createUnifiedDiff()`; LCS-алгоритм проверяет `signal.aborted` каждые N итераций; fallback на построчный diff; предотвращает зависание на файлах >5000 строк
- [ ] **11. Zod-схема для AgentSettings** — валидация при загрузке конфига через `z.parse()`; понятные сообщения об ошибках вместо runtime-падений; TypeScript-тип генерируется из схемы
- [ ] **12. Retry + backoff в selfCommit.ts** — обернуть git-операции в retry с задержкой 1→2→4 с, до 3 попыток; аналог логики в очереди агента
- [ ] **13. Батчинг запросов в embeddings.ts** — объединять конкурентные вызовы в `batchEmbeddings(texts[], limit=4)`; воркер уже есть, нет группировки входящих запросов
- [ ] **14. LRU-кэш grep_files в fileSearchInWorker.ts** — кэш по ключу `{pattern, dir, glob}` с инвалидацией по `mtime`; аналог LRU 500 записей для эмбеддингов
- [ ] **15. Настраиваемый порог суммаризации** — `CONTEXT_SUMMARIZE_THRESHOLD` в AgentSettings (50–85%, дефолт 85%); тумблер «Агрессивное сжатие» в настройках (65%); экономия 30–40% на длинных диалогах
- [ ] **16. Кнопка «Сжать историю» в превью контекста** — при заполнении > 60% показывать рекомендацию в `AgentContextBar`; кнопка запускает принудительную суммаризацию через IPC-вызов `summarize-context`
- [ ] **17. Удаление старых вызовов инструментов из контекста** — оставлять результаты только последних K вызовов (дефолт 5); более старые схлопываются в `[результат обрезан]`; экономия 10–15%
- [ ] **18. Автообрезка вложенных файлов** — файл > N КБ (дефолт 20 КБ): первые + последние 50 строк с маркером `... (X строк обрезано) ...`
- [ ] **19. Провайдер Groq** — совместим с OpenAI API; переиспользовать `openaiProvider` с другим `baseUrl`; отдельный пункт в UI
- [ ] **20. Провайдер Together AI** — аналогично п. 19; отдельный пункт в UI
- [ ] **21. Инструмент `trigger_github_workflow`** — `gh workflow run {workflow_id}`; просмотр статуса последних runs; GitHub token уже есть в настройках
- [ ] **22. Инструмент `create_issue`** — создание задач в GitHub Issues через `gh issue create`; поля: title, body, labels
- [ ] **23. UI: индикатор работы в других чатах** — иконка/пульс рядом с чатом в списке, если в нём сейчас работает агент
- [ ] **24. MemoryStorage interface для memory.ts** — изолировать `fs.readFile`/`fs.writeFile` за интерфейсом; в тестах подменяется in-memory реализацией
- [ ] **25. Поддержка альтернативных моделей оркестратора** — кнопка «Выбрать свою модель» → `dialog.showOpenDialog` с фильтром `.gguf`; путь сохраняется в `config.json`

---

### 🟠 Сложные — новый модуль или архитектурное изменение, 1–3 дня

- [ ] **26. Провайдер Claude (Anthropic SDK)** — установить `@anthropic-ai/sdk`; адаптер `claudeProvider.ts` по образцу `openaiProvider.ts`; UI: пункт «Claude» + поле API key
- [ ] **27. Провайдер Gemini** — `@google/generative-ai`; адаптер `geminiProvider.ts`; маппинг tool calling в формат Gemini
- [ ] **28. Очередь сообщений per-chat** — `enqueuedMessages` вынести в Map по chatId; агенты в разных чатах не блокируют друг друга
- [ ] **29. Каждый чат хранит свой projectPath** — агент получает путь из чата, не из глобального состояния; UI переключает проект без перезагрузки
- [ ] **30. Интеграция с Jira** — REST API: `POST /rest/api/3/issue`; Jira URL и API token в настройках (новая вкладка)
- [ ] **31. Интеграция с Linear** — GraphQL API: мутация `issueCreate`; API key в настройках
- [ ] **32. CI → Release pipeline** — GitHub Actions: при push тега `v*` → `electron-builder --publish always` → GitHub Releases; артефакты: `.exe`, `.dmg`, `.AppImage`
- [ ] **33. `electron-updater`** — проверяет GitHub Releases при старте; скачивает в фоне; баннер «Обновление готово» с кнопкой перезапуска
- [ ] **34. Кроссплатформенность** — `.sh` launcher для Linux/macOS; CI-матрица `windows-latest` + `ubuntu-latest` + `macos-latest`; условные пути в main process
- [ ] **35. Docker-образ для разработки** — `Dockerfile` с Node.js + Ollama; `docker-compose.yml` с hot reload
- [ ] **36. Виртуализация сообщений в чате** — внедрить `@tanstack/react-virtual` для основного списка в `ChatPanel`; виртуализация сейчас только для истории и памяти
- [ ] **37. Секция «Производительность» в настройках** — новая вкладка: тумблер «Режим энергосбережения» (батчинг 300 мс, без анимаций), тумблер «Отключить CPU/GPU-статы», тумблер «Обновлять PR только вручную»
- [ ] **38. Динамический системный промпт** — разбить `BASE_SYSTEM_PROMPT` на базовый (~200 токенов) + модули (инструменты, безопасность, Git); в режиме Chat — только базовый; экономия 10–20% на запрос
- [ ] **39. Отключение инструментов per-project** — чекбоксы по группам инструментов в настройках проекта; отключённые не включаются в список tools при запросе
- [ ] **40. Базовый класс StreamingChatProvider** — вынести общую логику стриминга из `ollamaProvider.ts` и `openaiProvider.ts`; конкретные провайдеры реализуют только `buildRequest()` и `parseChunk()`
- [ ] **41. Circuit breaker для API-провайдеров** — closed → open (5 подряд ошибок) → half-open (через 30 с); в состоянии open запросы отклоняются немедленно; статус виден в топбаре
- [ ] **42. Интеграционные тесты провайдеров с mock-сервером** — `nock` или `msw`; тесты `ollamaProvider` и `openaiProvider` без сети; сценарии: tool call, stream, 429, разрыв соединения
- [ ] **43. Семантическая верификация в actionVerification.ts** — дополнить regexp-проверку вызовом модели для граничных случаев; снизить число ложных блокировок
- [ ] **44. Декомпозиция AgentRunner** — `agent.ts` ≈1300 строк; вынести `ContextManager`, `ToolExecutor`, `SelfImprovementOrchestrator`, `LoopGuard`, `ResponseEmitter`; каждый класс ≤300 строк

---

### 🔴 Масштабные — новая инфраструктура, неделя и более

- [ ] **45. Qdrant: настройки подключения** — вкладка в настройках: URL + API key; проверка соединения; `qdrant-client` в зависимостях
- [ ] **46. Индексирование проекта в Qdrant** — инструмент `index_project`: чанкует файлы, получает эмбеддинги, пишет в коллекцию; индикатор прогресса
- [ ] **47. Инструмент `search_knowledge_base`** — семантический поиск по проиндексированному проекту; возвращает top-N чанков с путями
- [ ] **48. Поддержка Milvus** — альтернативный адаптер; выбор провайдера базы знаний в настройках
- [ ] **49. Парсинг схем MCP-серверов** — читать `/.well-known/mcp` или JSON-схему по URL; преобразовывать `tools[]` в формат `AgentTool`
- [ ] **50. Регистрация MCP-инструментов в агенте** — динамически добавлять инструменты из MCP в список tools перед каждым прогоном
- [ ] **51. UI добавления MCP-серверов** — вкладка в настройках: список серверов, кнопка «+ Добавить по URL», статус подключения
- [ ] **52. Передача результатов обратно в MCP** — `POST /mcp/tool_result` после выполнения; поддержка stateful-сессий
- [ ] **53. Загрузка плагинов из `~/.codeviper/plugins/`** — сканировать `*.ts`/`*.js` при старте; читать `export default { name, description, tools[] }`
- [ ] **54. Компиляция плагинов через esbuild** — бандлить в CommonJS в main process; кэшировать скомпилированный результат
- [ ] **55. Изолированное выполнение плагинов** — запускать в `worker_thread` с ограниченным API (`fs` только в projectPath, нет `net`)
- [ ] **56. UI управления плагинами** — вкладка в настройках: список, включить/выключить, инструменты плагина, кнопка «Открыть папку плагинов»
- [ ] **57. NSIS-установщик** — `electron-builder` + NSIS-скрипт; клонирует репозиторий в `%APPDATA%\CodeViper\source\`; ярлык на рабочем столе
- [ ] **58. Portable Node.js в комплекте** — скачивать Node.js в `resources/node/` при сборке; использовать для `npm run build` при самопересборке агента
- [ ] **59. Движок `node-llama-cpp` в Electron** — установить и скомпилировать нативный модуль под win32/linux/darwin; smoke-тест: загрузить GGUF, получить токен, выгрузить
- [ ] **60. Загрузчик модели оркестратора** — при первом старте скачивать `Qwen2.5-1.5B-Instruct.Q4_K_M.gguf` (~900 МБ) в `userData/orchestrator/`; прогресс-бар в UI; кнопка «Скачать» в настройках
- [ ] **61. Класс `OrchestratorModel`** — singleton в main process; `analyze(msg): { plan, rephrased, isComplex }`; без стриминга, только итог
- [ ] **62. Интеграция оркестратора в `AgentRunner`** — перед прогоном вызывать `orchestrator.analyze()`; план добавляется в системный промпт; если `isComplex=true` — использовать перефразированный запрос
- [ ] **63. UI: индикатор «Планирую...»** — stream-событие `orchestrating`; серый чип в чате; после — скрывать или показывать раскрывающийся блок с планом
- [ ] **64. Настройки оркестратора** — тумблер «Включить оркестратор»; порог `minMessageLength` (80 символов); кнопка «Удалить модель»
- [ ] **65. Сигнальный сервер P2P** — VPS с реестром узлов (ID, GPU/RAM, нагрузка); Node.js + WebSocket + Redis; REST API для регистрации и опроса
- [ ] **66. Аккаунтная система P2P** — регистрация по email/GitHub OAuth; JWT-токен определяет репутацию и лимиты
- [ ] **67. Лимит ресурсов узла P2P** — `systeminformation`; при отдаче не более 20% GPU и 15% CPU; при превышении — пауза новых задач
- [ ] **68. Режим «Поделиться мощностью»** — переключатель в настройках; узел регистрируется на сервере; принимает входящие задачи
- [ ] **69. Очередь входящих задач P2P** — не более 3 одновременно; приоритет по балансу кредитов; таймаут 60 с на задачу
- [ ] **70. Маршрутизация запросов P2P** — сервер ищет свободный узел с нужной моделью; если нет — откат на локальную Ollama
- [ ] **71. Шифрование трафика P2P** — TLS между узлами; end-to-end шифрование промптов; владелец узла не видит чужих данных
- [ ] **72. Система репутации P2P** — кредиты за отданные вычисления; трата при использовании чужих; история в UI
- [ ] **73. Экран согласия P2P** — явный диалог при первом включении с объяснением что именно будет передаваться

---

## ✅ Сделано

**Ядро агента**
- Стриминг, кнопка «Стоп», цикл `while(true)`, инструменты (create/edit/append/delete/move/grep/find/git), tool choice, парсинг text tool call, детектор опасных задач, защита `parseToolArgs`
- Рефакторинг `agent.ts` → 6 модулей; параллельное выполнение инструментов (Promise.all) при cloud API; удалены жёсткие лимиты шагов/прогонов
- Агент не молчит при пустом ответе; не останавливается на «намерении»; пропускает tool call для информационных вопросов

**Провайдеры и модели**
- Dual-provider режим (Ollama + cloud одновременно); нативный tool calling OpenAI/DeepSeek; `max_tokens`, `temperature` для cloud; работа без Ollama при cloud-провайдере
- Выбор модели в топбаре; динамический список моделей DeepSeek; совместимость моделей (✓/⚠ по RAM); управление моделями Ollama (каталог, автовыбор, скачать/удалить)
- Чип «Xс · Yk tok» в топбаре; оптимизация промпта и описаний инструментов

**Контекст и память**
- Суммаризация при ~85% лимита; адаптивные лимиты (`computeAdaptiveLimits`); чип «Инструмент: Xk | История: N»
- Самообучение: `remember`, рефлексия, план самоулучшения, anti-loop, эмбеддинги (LRU 500, worker)
- Черновики при обрыве стрима: `interruptedDraft`, баннер «Повторить»

**UI/UX**
- История чатов: папки, поиск, drag-and-drop, pin, теги, привязка к проекту; экспорт/импорт JSON+Markdown
- Вложения: файлы, drag-and-drop, Ctrl+V скриншоты, base64 для мультимодальных, лимит 10 файлов/200 КБ
- Группировка одинаковых вызовов инструментов в чате; открытие файлов из текста агента
- Подсветка кода, ANSI-цвета, анимации, горячие клавиши, звуковые уведомления, тёмная тема

**Архитектура фронтенда**
- `AgentContext` (`useReducer` + Context): phase, runStats, метрики — без пропсов
- `ChatContext`: messages, activeChatId, chatStore, activeChat, interruptedDraft — без пропсов
- `QueueContext`: chatBusy — без пропсов; единый `resetQueue()` сбрасывает и очередь, и стрим

**Производительность**
- Workers: grep/find, эмбеддинги, парсинг больших файлов — main не блокируется
- Батчинг IPC token/thinking с 50 мс flush; `setInterval` runStats снижен
- Виртуализация длинных списков (`@tanstack/react-virtual`); ленивая загрузка (`React.lazy`)

**Git и проекты**
- Параллельные агенты per-chat (`Map<chatId, ...>`); git-sync (stash/rebase/ff-only)
- Diff перед правками (`preview_edit`): LCS → unified diff → UI с «Применить»/«Отмена»
- История изменений файлов (NDJSON-лог, `show_file_history`); ветки и PR агента
- Отложенное применение правок вне режима самоулучшения (git stash); автовосстановление после краша

**Безопасность и инфраструктура**
- `run_command` без `shell: true`; `assertInsideProject`; blocklist; шифрование API-ключей (`safeStorage`)
- ESLint + Prettier + lint-staged + husky; vitest 37+ тестов; E2E Playwright+Electron; нагрузочные тесты
- Семантическое версионирование; GitHub Actions CI; branch protection; доступность (WCAG AA)
- Статус PR в UI (CI-статус, опрос 60 с); автодополнение в терминале; метрики (tok/s, NDJSON-лог)
