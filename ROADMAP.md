# Дорожная карта CodeViper

Планы развития и список выполненного. Назад в [README](README.md).

## 📋 В планах

Задачи отсортированы по сложности выполнения агентом: от однофайловых правок до отдельных инфраструктурных подсистем. Нумерация сквозная, начинается с 1.

---

### 🟡 Средние — несколько файлов, полдня–день

- [ ] **1. PR-панель: опрос только при открытой панели** — `PrStatusPanel` принимает `isOpen`; `setInterval` запускается только при `isOpen === true`; интервал с 60 с до 300 с
- [ ] **2. Кэш контекст-превью по хешу** — в `useContextPreview` хранить последний результат для пары `{messagesLength, model}`; пропускать IPC-запрос, если вход не изменился
- [ ] **3. AbortSignal-таймаут в diffUtil.ts** — передавать `AbortSignal` в `createUnifiedDiff()`; LCS-алгоритм проверяет `signal.aborted` каждые N итераций; fallback на построчный diff; предотвращает зависание на файлах >5000 строк
- [ ] **4. Zod-схема для AgentSettings** — валидация при загрузке конфига через `z.parse()`; понятные сообщения об ошибках вместо runtime-падений; TypeScript-тип генерируется из схемы
- [ ] **5. Retry + backoff в selfCommit.ts** — обернуть git-операции в retry с задержкой 1→2→4 с, до 3 попыток; аналог логики в очереди агента
- [ ] **6. Батчинг запросов в embeddings.ts** — объединять конкурентные вызовы в `batchEmbeddings(texts[], limit=4)`; воркер уже есть, нет группировки входящих запросов
- [ ] **7. LRU-кэш grep_files в fileSearchInWorker.ts** — кэш по ключу `{pattern, dir, glob}` с инвалидацией по `mtime`; аналог LRU 500 записей для эмбеддингов
- [ ] **8. Настраиваемый порог суммаризации** — `CONTEXT_SUMMARIZE_THRESHOLD` в AgentSettings (50–85%, дефолт 85%); тумблер «Агрессивное сжатие» в настройках (65%); экономия 30–40% на длинных диалогах
- [ ] **9. Кнопка «Сжать историю» в превью контекста** — при заполнении > 60% показывать рекомендацию в `AgentContextBar`; кнопка запускает принудительную суммаризацию через IPC-вызов `summarize-context`
- [ ] **10. Удаление старых вызовов инструментов из контекста** — оставлять результаты только последних K вызовов (дефолт 5); более старые схлопываются в `[результат обрезан]`; экономия 10–15%
- [ ] **11. Автообрезка вложенных файлов** — файл > N КБ (дефолт 20 КБ): первые + последние 50 строк с маркером `... (X строк обрезано) ...`
- [ ] **13. Провайдер Groq** — совместим с OpenAI API; переиспользовать `openaiProvider` с другим `baseUrl`; отдельный пункт в UI
- [ ] **14. Провайдер Together AI** — аналогично п. 15; отдельный пункт в UI
- [ ] **15. Инструмент `trigger_github_workflow`** — `gh workflow run {workflow_id}`; просмотр статуса последних runs; GitHub token уже есть в настройках
- [ ] **16. Инструмент `create_issue`** — создание задач в GitHub Issues через `gh issue create`; поля: title, body, labels
- [ ] **17. UI: индикатор работы в других чатах** — иконка/пульс рядом с чатом в списке, если в нём сейчас работает агент
- [ ] **18. MemoryStorage interface для memory.ts** — изолировать `fs.readFile`/`fs.writeFile` за интерфейсом; в тестах подменяется in-memory реализацией
- [ ] **19. Поддержка альтернативных моделей оркестратора** — кнопка «Выбрать свою модель» → `dialog.showOpenDialog` с фильтром `.gguf`; путь сохраняется в `config.json`

---

### 🟠 Сложные — новый модуль или архитектурное изменение, 1–3 дня

- [ ] **20. Провайдер Claude (Anthropic SDK)** — установить `@anthropic-ai/sdk`; адаптер `claudeProvider.ts` по образцу `openaiProvider.ts`; UI: пункт «Claude» + поле API key
- [ ] **21. Провайдер Gemini** — `@google/generative-ai`; адаптер `geminiProvider.ts`; маппинг tool calling в формат Gemini
- [ ] **22. Очередь сообщений per-chat** — `enqueuedMessages` вынести в Map по chatId; агенты в разных чатах не блокируют друг друга
- [ ] **23. Каждый чат хранит свой projectPath** — агент получает путь из чата, не из глобального состояния; UI переключает проект без перезагрузки
- [ ] **24. Интеграция с Jira** — REST API: `POST /rest/api/3/issue`; Jira URL и API token в настройках (новая вкладка)
- [ ] **25. Интеграция с Linear** — GraphQL API: мутация `issueCreate`; API key в настройках
- [ ] **26. CI → Release pipeline** — GitHub Actions: при push тега `v*` → `electron-builder --publish always` → GitHub Releases; артефакты: `.exe`, `.dmg`, `.AppImage`
- [ ] **27. `electron-updater`** — проверяет GitHub Releases при старте; скачивает в фоне; баннер «Обновление готово» с кнопкой перезапуска
- [ ] **28. Кроссплатформенность** — `.sh` launcher для Linux/macOS; CI-матрица `windows-latest` + `ubuntu-latest` + `macos-latest`; условные пути в main process
- [ ] **29. Docker-образ для разработки** — `Dockerfile` с Node.js + Ollama; `docker-compose.yml` с hot reload
- [ ] **30. Виртуализация сообщений в чате** — внедрить `@tanstack/react-virtual` для основного списка в `ChatPanel`; виртуализация сейчас только для истории и памяти
- [ ] **31. Секция «Производительность» в настройках** — новая вкладка: тумблер «Режим энергосбережения» (батчинг 300 мс, без анимаций), тумблер «Отключить CPU/GPU-статы», тумблер «Обновлять PR только вручную»
- [ ] **32. Динамический системный промпт** — разбить `BASE_SYSTEM_PROMPT` на базовый (~200 токенов) + модули (инструменты, безопасность, Git); в режиме Chat — только базовый; экономия 10–20% на запрос
- [ ] **33. Отключение инструментов per-project** — чекбоксы по группам инструментов в настройках проекта; отключённые не включаются в список tools при запросе
- [ ] **34. Базовый класс StreamingChatProvider** — вынести общую логику стриминга из `ollamaProvider.ts` и `openaiProvider.ts`; конкретные провайдеры реализуют только `buildRequest()` и `parseChunk()`
- [ ] **35. Circuit breaker для API-провайдеров** — closed → open (5 подряд ошибок) → half-open (через 30 с); в состоянии open запросы отклоняются немедленно; статус виден в топбаре
- [ ] **36. Интеграционные тесты провайдеров с mock-сервером** — `nock` или `msw`; тесты `ollamaProvider` и `openaiProvider` без сети; сценарии: tool call, stream, 429, разрыв соединения
- [ ] **37. Семантическая верификация в actionVerification.ts** — дополнить regexp-проверку вызовом модели для граничных случаев; снизить число ложных блокировок
- [ ] **38. Декомпозиция AgentRunner** — `agent.ts` ≈1300 строк; вынести `ContextManager`, `ToolExecutor`, `SelfImprovementOrchestrator`, `LoopGuard`, `ResponseEmitter`; каждый класс ≤300 строк

---

### 🔴 Масштабные — новая инфраструктура, неделя и более

- [ ] **39. Qdrant: настройки подключения** — вкладка в настройках: URL + API key; проверка соединения; `qdrant-client` в зависимостях
- [ ] **40. Индексирование проекта в Qdrant** — инструмент `index_project`: чанкует файлы, получает эмбеддинги, пишет в коллекцию; индикатор прогресса
- [ ] **41. Инструмент `search_knowledge_base`** — семантический поиск по проиндексированному проекту; возвращает top-N чанков с путями
- [ ] **42. Поддержка Milvus** — альтернативный адаптер; выбор провайдера базы знаний в настройках
- [ ] **43. Парсинг схем MCP-серверов** — читать `/.well-known/mcp` или JSON-схему по URL; преобразовывать `tools[]` в формат `AgentTool`
- [ ] **44. Регистрация MCP-инструментов в агенте** — динамически добавлять инструменты из MCP в список tools перед каждым прогоном
- [ ] **45. UI добавления MCP-серверов** — вкладка в настройках: список серверов, кнопка «+ Добавить по URL», статус подключения
- [ ] **46. Передача результатов обратно в MCP** — `POST /mcp/tool_result` после выполнения; поддержка stateful-сессий
- [ ] **47. Загрузка плагинов из `~/.codeviper/plugins/`** — сканировать `*.ts`/`*.js` при старте; читать `export default { name, description, tools[] }`
- [ ] **48. Компиляция плагинов через esbuild** — бандлить в CommonJS в main process; кэшировать скомпилированный результат
- [ ] **49. Изолированное выполнение плагинов** — запускать в `worker_thread` с ограниченным API (`fs` только в projectPath, нет `net`)
- [ ] **50. UI управления плагинами** — вкладка в настройках: список, включить/выключить, инструменты плагина, кнопка «Открыть папку плагинов»
- [ ] **51. NSIS-установщик** — `electron-builder` + NSIS-скрипт; клонирует репозиторий в `%APPDATA%\CodeViper\source\`; ярлык на рабочем столе
- [ ] **52. Portable Node.js в комплекте** — скачивать Node.js в `resources/node/` при сборке; использовать для `npm run build` при самопересборке агента
- [ ] **53. Движок `node-llama-cpp` в Electron** — установить и скомпилировать нативный модуль под win32/linux/darwin; smoke-тест: загрузить GGUF, получить токен, выгрузить
- [ ] **54. Загрузчик модели оркестратора** — при первом старте скачивать `Qwen2.5-1.5B-Instruct.Q4_K_M.gguf` (~900 МБ) в `userData/orchestrator/`; прогресс-бар в UI; кнопка «Скачать» в настройках
- [ ] **55. Класс `OrchestratorModel`** — singleton в main process; `analyze(msg): { plan, rephrased, isComplex }`; без стриминга, только итог
- [ ] **56. Интеграция оркестратора в `AgentRunner`** — перед прогоном вызывать `orchestrator.analyze()`; план добавляется в системный промпт; если `isComplex=true` — использовать перефразированный запрос
- [ ] **57. UI: индикатор «Планирую...»** — stream-событие `orchestrating`; серый чип в чате; после — скрывать или показывать раскрывающийся блок с планом
- [ ] **58. Настройки оркестратора** — тумблер «Включить оркестратор»; порог `minMessageLength` (80 символов); кнопка «Удалить модель»
- [ ] **59. Сигнальный сервер P2P** — VPS с реестром узлов (ID, GPU/RAM, нагрузка); Node.js + WebSocket + Redis; REST API для регистрации и опроса
- [ ] **60. Аккаунтная система P2P** — регистрация по email/GitHub OAuth; JWT-токен определяет репутацию и лимиты
- [ ] **61. Лимит ресурсов узла P2P** — `systeminformation`; при отдаче не более 20% GPU и 15% CPU; при превышении — пауза новых задач
- [ ] **62. Режим «Поделиться мощностью»** — переключатель в настройках; узел регистрируется на сервере; принимает входящие задачи
- [ ] **63. Очередь входящих задач P2P** — не более 3 одновременно; приоритет по балансу кредитов; таймаут 60 с на задачу
- [ ] **64. Маршрутизация запросов P2P** — сервер ищет свободный узел с нужной моделью; если нет — откат на локальную Ollama
- [ ] **65. Шифрование трафика P2P** — TLS между узлами; end-to-end шифрование промптов; владелец узла не видит чужих данных
- [ ] **66. Система репутации P2P** — кредиты за отданные вычисления; трата при использовании чужих; история в UI
- [ ] **67. Экран согласия P2P** — явный диалог при первом включении с объяснением что именно будет передаваться

---

## ✅ Сделано

**UI**
- Убран заголовок активного чата из топбара (показывал внутренние задачи самоулучшения); иконка приложения из `resources/icon.png`; логотип PNG в топбаре вместо эмодзи
- Раздельные API-ключи для DeepSeek / OpenAI / OpenRouter (шифрование `safeStorage`); миграция со старого единого `providerApiKey`
- Список моделей OpenRouter с фильтром tool calling, поиском и отображением размера контекста
- Раздельные списки чатов для вкладок Chat и Code (`SavedChat.mode`); кнопки чата скрыты по умолчанию, появляются при наведении

**Производительность**
- Интервал опроса CPU/GPU снижен с 1 с до 3 с (`systemStats.ts`)
- Устойчивость при смене монитора: флаги `--disable-gpu-process-crash-limit` + `--in-process-gpu`; авто-reload рендерера при крашах GPU/renderer → `CrashRecoveryDialog` восстанавливает сессию

**Ядро агента**
- Очистка editSnapshots при старте каждого прогона — предотвращает утечку памяти в длинных сессиях
- Стриминг, кнопка «Стоп», цикл `while(true)`, инструменты (create/edit/append/delete/move/grep/find/git), tool choice, парсинг text tool call, детектор опасных задач, защита `parseToolArgs`
- Рефакторинг `agent.ts` → 6 модулей; параллельное выполнение инструментов (Promise.all) при cloud API; удалены жёсткие лимиты шагов/прогонов
- Агент не молчит при пустом ответе; не останавливается на «намерении»; пропускает tool call для информационных вопросов

**Провайдеры и модели**
- Dual-provider режим (Ollama + cloud одновременно); нативный tool calling OpenAI/DeepSeek; `max_tokens`, `temperature` для cloud; работа без Ollama при cloud-провайдере
- Провайдер OpenRouter (агрегатор: GPT-4o, Claude, Gemini, Llama и др.) — основной и облачный
- Выбор модели в топбаре; динамический список моделей DeepSeek; совместимость моделей (✓/⚠ по RAM); управление моделями Ollama (каталог, автовыбор, скачать/удалить)
- Статистика «Xс · Yk токенов» под последним сообщением агента; пульсирующая индикаторная полоса вместо бегущей; убрана нагрузка CPU/GPU из статусбара; оптимизация промпта и описаний инструментов

**Контекст и память**
- Исключение reasoning (<think>...</think>) из истории контекста: опция в настройках (вкладка Модель), экономия 20–50% токенов для think-моделей
- Суммаризация при ~85% лимита; адаптивные лимиты (`computeAdaptiveLimits`); чип «Инструмент: Xk | История: N»
- Самообучение: `remember`, рефлексия, план самоулучшения, anti-loop, эмбеддинги (LRU 500, worker)
- Фикс гонки инициализации в `embeddingQueue.ts`: буферизация запросов до `ready`-события воркера, очистка очереди при падении воркера; 9 vitest-тестов
- Корректный контекст для облачных моделей: `getModelContextLimitTokens` распознаёт deepseek-*, gpt-*, claude-*, gemini-*; `modelContextLength` из API сохраняется в настройках и прокидывается до `computeContextUsage`
- Оптимизация токенов: `getAgentTools(selfImproveMode)` исключает 19 инструментов (codeviper_*, ollama model, self-improve plan) в обычном режиме — экономия ~35% tools JSON на каждый запрос; `buildSelfEditContext()` тоже добавляется только в self-improve
- Новый инструмент `search_in_file`: поиск текста в одном конкретном файле без ограничения по размеру (включает файлы >512KB)
- `grep_files` теперь явно сообщает о пропущенных файлах >512KB и предлагает использовать `search_in_file`
- Скилл Todo List для агента: инструменты `set_todo_list`, `complete_todo_item`, `clear_todo_list`; компактная панель `TodoPanel` прикреплена над полем ввода, обновляется через stream-событие `todo_update`
- Черновики при обрыве стрима: `interruptedDraft`, баннер «Повторить»

**UI/UX**
- История чатов: папки, поиск, drag-and-drop, pin, теги, привязка к проекту; экспорт/импорт JSON+Markdown
- Вложения: файлы, drag-and-drop, Ctrl+V скриншоты, base64 для мультимодальных, лимит 10 файлов/200 КБ
- Группировка одинаковых вызовов инструментов в чате; открытие файлов из текста агента
- Подсветка кода, ANSI-цвета, анимации, горячие клавиши, звуковые уведомления, тёмная тема

**Архитектура фронтенда**
- `AgentContext` (`useReducer` + Context): phase, runStats, метрики — без пропсов
- `ChatContext`: messages, activeChatId, chatStore, activeChat, interruptedDraft — без пропсов
- `QueueContext`: chatBusy — без пропсов; единый `resetQueue()` сбрасывает и очередь, и стрим

**Производительность**
- Workers: grep/find, эмбеддинги, парсинг больших файлов — main не блокируется
- Батчинг IPC token/thinking; батчинг токенов в UI (150 мс flush); дебаунс контекст-превью 1000 мс; `setInterval` runStats снижен
- `React.memo` на `MessageBody`; виртуализация длинных списков (`@tanstack/react-virtual`); ленивая загрузка (`React.lazy`)
- Защита от зацикливания: подсказка модели + продолжение прогона без остановки (лимиты шагов/попыток удалены)

**Git и проекты**
- Параллельные агенты per-chat (`Map<chatId, ...>`); git-sync (stash/rebase/ff-only)
- Diff перед правками (`preview_edit`): LCS → unified diff → UI с «Применить»/«Отмена»
- История изменений файлов (NDJSON-лог, `show_file_history`); ветки и PR агента
- Отложенное применение правок вне режима самоулучшения (git stash); автовосстановление после краша

**Безопасность и инфраструктура**
- `run_command` без `shell: true`; `assertInsideProject`; blocklist; шифрование API-ключей (`safeStorage`)
- ESLint + Prettier + lint-staged + husky; vitest 37+ тестов; E2E Playwright+Electron; нагрузочные тесты
- Семантическое версионирование; GitHub Actions CI; branch protection; доступность (WCAG AA)
- Статус PR в UI (CI-статус, опрос 60 с); автодополнение в терминале; метрики (tok/s, NDJSON-лог)
