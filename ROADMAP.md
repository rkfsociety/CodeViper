# Дорожная карта CodeViper

Планы развития и список выполненного. Назад в [README](README.md).

## 📋 В планах

Задачи сгруппированы по теме, внутри каждой группы — от лёгких к сложным. Нумерация начинается с 1.

---

### 🔌 Провайдеры моделей

- [ ] **1. Провайдер Claude (Anthropic SDK)** — установить `@anthropic-ai/sdk`; адаптер `claudeProvider.ts` по образцу `openaiProvider.ts`; UI: пункт «Claude» в списке провайдеров + поле API key
- [ ] **2. Провайдер Gemini** — `@google/generative-ai`; адаптер `geminiProvider.ts`; маппинг tool calling в формат Gemini; UI аналогично п. 1
- [ ] **3. Провайдер Groq** — совместим с OpenAI API; переиспользовать `openaiProvider` с другим `baseUrl`; добавить в список провайдеров как отдельный пункт
- [ ] **4. Провайдер Together AI** — аналогично п. 3 (OpenAI-совместимый); отдельный пункт в UI

---

### 💬 Параллельные чаты и проекты

- [ ] **5. Очередь сообщений per-chat** — `enqueuedMessages` вынести в Map по chatId; агенты в разных чатах не блокируют друг друга
- [ ] **6. UI: индикатор работы в других чатах** — иконка/пульс рядом с чатом в списке, если в нём сейчас работает агент
- [ ] **7. Каждый чат хранит свой projectPath** — `SavedChat.projectPath` уже есть; агент получает его из чата, а не из глобального состояния; UI показывает/переключает проект без перезагрузки

---

### 🔗 Внешние интеграции

- [ ] **8. Инструмент `trigger_github_workflow`** — `gh workflow run {workflow_id}`; просмотр статуса последних runs; GitHub token уже есть в настройках
- [ ] **9. Инструмент `create_issue`** — создание задач в GitHub Issues через `gh issue create`; поля: title, body, labels
- [ ] **10. Интеграция с Jira** — REST API: создать задачу `POST /rest/api/3/issue`; Jira URL и API token в настройках (новая вкладка)
- [ ] **11. Интеграция с Linear** — GraphQL API: мутация `issueCreate`; API key в настройках

---

### 🧠 Внешние базы знаний

- [ ] **12. Qdrant: настройки подключения** — вкладка в настройках: URL + API key; проверка соединения; `qdrant-client` в зависимостях
- [ ] **13. Индексирование проекта в Qdrant** — инструмент `index_project`: чанкует файлы, получает эмбеддинги, пишет в коллекцию; индикатор прогресса
- [ ] **14. Инструмент `search_knowledge_base`** — семантический поиск по проиндексированному проекту; возвращает top-N чанков с путями
- [ ] **15. Поддержка Milvus** — альтернативный адаптер; выбор провайдера базы знаний в настройках

---

### 🔌 MCP (Model Context Protocol)

- [ ] **16. Парсинг схем MCP-серверов** — читать `/.well-known/mcp` или JSON-схему по URL; преобразовывать `tools[]` в формат `AgentTool`
- [ ] **17. Регистрация MCP-инструментов в агенте** — динамически добавлять инструменты из MCP в список tools перед каждым прогоном
- [ ] **18. UI добавления MCP-серверов** — вкладка в настройках: список серверов, кнопка «+ Добавить по URL», статус подключения
- [ ] **19. Передача результатов обратно в MCP** — `POST /mcp/tool_result` после выполнения; поддержка stateful-сессий

---

### 🧩 Плагины

- [ ] **20. Загрузка плагинов из `~/.codeviper/plugins/`** — сканировать `*.ts`/`*.js` при старте; читать `export default { name, description, tools[] }`
- [ ] **21. Компиляция плагинов через esbuild** — запускать `esbuild` в main process; бандлить в CommonJS; кэшировать скомпилированный результат
- [ ] **22. Изолированное выполнение плагинов** — запускать в отдельном `worker_thread` с ограниченным API (`fs` только в projectPath, нет `net`)
- [ ] **23. UI управления плагинами** — вкладка в настройках: список, включить/выключить, просмотр инструментов плагина, кнопка «Открыть папку плагинов»

---

### 📦 Сборка и дистрибуция

- [ ] **24. CI → Release pipeline** — GitHub Actions: при push тега `v*` запускает `electron-builder --publish always` → GitHub Releases; артефакты: `.exe`, `.dmg`, `.AppImage`
- [ ] **25. `electron-updater`** — проверяет GitHub Releases при старте; скачивает в фоне; показывает баннер «Обновление готово» с кнопкой перезапуска
- [ ] **26. NSIS-установщик** — `electron-builder` + NSIS-скрипт; при установке клонирует репозиторий в `%APPDATA%\CodeViper\source\`; ярлык на рабочем столе
- [ ] **27. Portable Node.js в комплекте** — при сборке скачивать Node.js в `resources/node/`; использовать его для `npm run build` при самопересборке агента
- [ ] **28. Кроссплатформенность** — `.sh` launcher для Linux/macOS; CI-матрица `windows-latest` + `ubuntu-latest` + `macos-latest`; условные пути в main process
- [ ] **29. Docker-образ для разработки** — `Dockerfile` с Node.js + Ollama; `docker-compose.yml` с hot reload; инструкция в README

---

### 🧭 Встроенный оркестратор (embedded model)

Лёгкая модель (~1 ГБ), вшитая в агента — работает без Ollama и LM Studio. Разбирает намерение пользователя, строит план и перефразирует запрос для основной модели. Суть запроса не меняет.

- [ ] **30. Движок `node-llama-cpp` в Electron** — установить `node-llama-cpp`; скомпилировать нативный модуль под win32/linux/darwin в `postinstall`; smoke-тест: загрузить GGUF, получить токен, выгрузить
- [ ] **31. Загрузчик модели оркестратора** — при первом старте скачивать `Qwen2.5-1.5B-Instruct.Q4_K_M.gguf` (~900 МБ) в `userData/orchestrator/`; прогресс-бар в UI; кнопка «Скачать оркестратор» в настройках; хранить путь в `config.json`
- [ ] **32. Класс `OrchestratorModel`** — singleton в main process; методы `analyze(userMessage): { plan: string[], rephrased: string, isComplex: boolean }`; системный промпт: «Ты планировщик. Составь шаги выполнения задачи. Перефразируй запрос для исполнителя — кратко, конкретно, без потери смысла»; использует `node-llama-cpp` без стриминга (только итог)
- [ ] **33. Интеграция в `AgentRunner`** — перед `prepareAgentRunContext` вызывать `orchestrator.analyze(userMessage)`; результат добавлять в системный промпт блоком `## План оркестратора`; если `isComplex=true` — использовать перефразированный запрос вместо исходного как подсказку для основной модели (исходный сохраняется в истории как есть)
- [ ] **34. UI: индикатор «Планирую...»** — stream-событие `orchestrating` в `AgentStreamPayload`; в чате показывать серый чип «🧭 Составляю план...» пока оркестратор думает; после — скрывать или показывать раскрывающийся блок с планом
- [ ] **35. Настройки оркестратора** — тумблер «Включить оркестратор» в настройках (вкладка Модель); порог `minMessageLength` (по умолчанию 80 символов) — короткие сообщения пропускаются без анализа; кнопка «Удалить модель» для освобождения места
- [ ] **36. Поддержка альтернативных моделей** — в настройках кнопка «Выбрать свою модель» → `dialog.showOpenDialog` с фильтром `.gguf`; путь сохраняется в `config.json`; при старте проверяется существование файла

---

### 🚀 Производительность

- [ ] **37. Батчинг токенов 80 мс → 150 мс** — в `useAgentStream` заменить `setTimeout(flushPending, 80)` на 150; снижение CPU-нагрузки при Markdown-рендере длинных ответов на ~30–40%
- [ ] **38. Дебаунс контекст-превью 600 мс → 1000 мс** — изменить `CONTEXT_PREVIEW_DEBOUNCE_MS` в `shared/constants.ts`; меньше IPC-запросов при быстром наборе текста
- [ ] **39. PR-панель: опрос только при открытой панели** — `PrStatusPanel` принимает `isOpen` проп; `setInterval` запускается только при `isOpen === true`; интервал увеличить с 60 с до 300 с
- [ ] **40. React.memo на MessageBody** — обернуть `export function MessageBody` в `memo()`; `MessageRow` уже в `memo`, `MessageBody` нет; устраняет повторный Markdown-рендер при изменении других сообщений
- [ ] **41. Снизить частоту опроса системных статов до 3 с** — в main-процессе интервал сбора CPU/GPU уменьшить с 1 с до 3 с; для индикатора достаточно, CPU-нагрузка снижается
- [ ] **42. Кэш контекст-превью по хешу** — в `useContextPreview` хранить последний результат для пары `{messagesLength, model}`; пропускать IPC-запрос, если вход не изменился; особенно заметно при повторных открытиях модала контекста
- [ ] **43. Виртуализация сообщений в чате** — внедрить `@tanstack/react-virtual` для основного списка сообщений в `ChatPanel`; сейчас виртуализация есть только для истории чатов и памяти; значимый прирост при чатах > 100 сообщений
- [ ] **44. Секция «Производительность» в настройках** — новая вкладка в `SettingsModal`: тумблер «Режим энергосбережения» (батчинг 300 мс, отключить анимации), тумблер «Отключить CPU/GPU-статы», тумблер «Обновлять PR только вручную»

---

### 💡 Оптимизация токенов

- [ ] **45. Настраиваемый порог суммаризации** — сделать `CONTEXT_SUMMARIZE_THRESHOLD` настраиваемым через AgentSettings (диапазон 50–85%, дефолт 85%); тумблер «Агрессивное сжатие истории» в настройках (устанавливает 65%); экономия 30–40% на длинных диалогах
- [ ] **46. Кнопка «Сжать историю» в превью контекста** — в `AgentContextBar` при заполнении > 60% показывать рекомендацию; кнопка запускает принудительную суммаризацию без ожидания порога; IPC-вызов `summarize-context` по требованию
- [ ] **47. Исключить thinking из истории** — при формировании контекста в `agentContext.ts` не включать поле `thinking` из `ChatMessage` в передаваемую историю; опция «Не сохранять рассуждения в контексте» в настройках (вкладка Модель); экономия 20–50% для think-моделей типа DeepSeek-R1
- [ ] **48. Удаление старых вызовов инструментов из контекста** — при построении контекста оставлять результаты только последних K вызовов (настраиваемо, дефолт 5); более старые схлопываются в плейсхолдер «[результат обрезан]»; экономия 10–15%
- [ ] **49. Автообрезка вложенных файлов** — если файл > N КБ (дефолт 20 КБ): передавать первые + последние 50 строк с маркером `... (X строк обрезано) ...`; для кода работает лучше, чем слепое усечение по байтам
- [ ] **50. Динамический системный промпт** — разбить `BASE_SYSTEM_PROMPT` на базовый (~200 токенов) + модули (инструменты, безопасность, Git); в режиме Chat подключать только базовый + коммуникационный; в Code — полный набор; экономия 10–20% на каждом запросе
- [ ] **51. Отключение инструментов per-project** — в настройках проекта чекбоксы по группам инструментов (Git, npm/yarn, файловые операции, bash-команды); отключённые инструменты не включаются в список tools при формировании запроса, снижая размер системного промпта

---

### 🌐 P2P-сеть вычислений

- [ ] **52. Сигнальный сервер** — VPS с реестром узлов (ID, GPU/RAM, нагрузка); Node.js + WebSocket + Redis; REST API для регистрации и опроса
- [ ] **53. Аккаунтная система** — регистрация по email/GitHub OAuth; JWT-токен определяет репутацию и лимиты
- [ ] **54. Лимит ресурсов узла** — `systeminformation`; при отдаче не более 20% GPU и 15% CPU; при превышении — пауза новых задач
- [ ] **55. Режим «Поделиться мощностью»** — переключатель в настройках; узел регистрируется на сервере и принимает входящие задачи
- [ ] **56. Очередь входящих задач** — не более 3 одновременно; приоритет по балансу кредитов; таймаут 60 с на задачу
- [ ] **57. Маршрутизация запросов** — сервер ищет свободный узел с нужной моделью; если нет — откат на локальную Ollama
- [ ] **58. Шифрование трафика** — TLS между узлами; end-to-end шифрование промптов; владелец узла не видит чужих данных
- [ ] **59. Система репутации** — кредиты за отданные вычисления; трата при использовании чужих; история в UI
- [ ] **60. Экран согласия** — явный диалог при первом включении режима P2P с объяснением что именно будет передаваться

---

### 🔧 Качество кода

- [ ] **61. Zod-схема для AgentSettings** — валидация `AgentSettings` при загрузке конфига через `z.parse()`; понятные сообщения об ошибках вместо runtime-падений при кривом `config.json`; генерация TypeScript-типа из схемы заменяет ручной интерфейс
- [ ] **62. Retry + backoff в selfCommit.ts** — обернуть git-операции (commit, push, stash) в retry с экспоненциальной задержкой (1 с → 2 с → 4 с, до 3 попыток); аналог существующей логики в очереди агента; снижает число сбоев при одновременных git-операциях
- [ ] **63. Батчинг запросов в embeddings.ts** — объединять конкурентные вызовы в `batchEmbeddings(texts[], limit=4)`; не более 4 параллельных запросов к Ollama embed; воркер уже есть, нет группировки входящих запросов
- [ ] **64. Базовый класс StreamingChatProvider** — вынести общую логику стриминга (accumulate delta, flush on done, handle errors) из `ollamaProvider.ts` и `openaiProvider.ts` в абстрактный класс; конкретные провайдеры реализуют только `buildRequest()` и `parseChunk()`
- [ ] **65. MemoryStorage interface для memory.ts** — изолировать `fs.readFile`/`fs.writeFile` за интерфейсом `MemoryStorage`; в тестах подменяется in-memory реализацией; устраняет необходимость мокать глобальный `fs` в unit-тестах памяти
- [ ] **66. Семантическая верификация в actionVerification.ts** — дополнить regexp-проверку вызовом модели для граничных случаев (неоднозначные команды, скрытые деструктивные операции); снижает число ложных блокировок и пропусков опасных паттернов
- [ ] **67. Интеграционные тесты провайдеров с mock-сервером** — `nock` или `msw` для перехвата HTTP; тесты `ollamaProvider` и `openaiProvider` на реальных HTTP-запросах без сети; сценарии: tool call, stream, ошибка 429, разрыв соединения
- [ ] **68. Дальнейшая декомпозиция AgentRunner** — `agent.ts` ≈1300 строк; вынести `ContextManager` (сборка истории), `ToolExecutor` (вызов и парсинг), `SelfImprovementOrchestrator` (ветка саморедактирования), `LoopGuard` (детектор циклов), `ResponseEmitter` (стриминг событий); каждый класс ≤300 строк

---

### 🛡️ Надёжность

- [ ] **69. LRU-кэш grep_files в fileSearchInWorker.ts** — кэш результатов `grep_files` по ключу `{pattern, dir, glob}` с инвалидацией по `mtime` файлов; воркер уже есть (аналог LRU 500 записей для эмбеддингов); ускорение повторных поисков по большому проекту
- [ ] **70. Фикс гонки инициализации в embeddingQueue.ts** — ленивая инициализация воркера без блокировки: запросы, пришедшие до события `online`, кладутся в очередь и отправляются сразу после готовности listener'а; сейчас они теряются
- [ ] **71. AbortSignal-таймаут в diffUtil.ts** — передавать `AbortSignal` в `createUnifiedDiff()`; LCS-алгоритм проверяет `signal.aborted` каждые N итераций; при срабатывании — fallback на построчный diff без LCS; предотвращает зависание UI на файлах >5000 строк
- [ ] **72. Circuit breaker для API-провайдеров** — три состояния: closed → open (после 5 подряд ошибок) → half-open (через 30 с); в состоянии open запросы отклоняются немедленно с сообщением пользователю; статус виден в топбаре рядом с индикатором модели
- [ ] **73. Очистка editSnapshots при старте прогона** — в `agentHandlersProject.ts` сбрасывать Map `editSnapshots` при каждом новом `runId`; сейчас снапшоты накапливаются за всё время сессии и не освобождаются; утечка заметна в длинных сессиях с интенсивным редактированием файлов

---

## ✅ Сделано

- [x] **Diff перед правками (preview_edit)** — инструмент `preview_edit {path, content}` строит unified diff через LCS-алгоритм → stream-событие `preview`; UI показывает diff-блок с подсветкой строк и кнопками «Применить» / «Отмена»; pending-preview Map + IPC back-channel
- [x] **История изменений файлов** — NDJSON-лог в `userData/logs/file-history-YYYY-MM-DD.ndjson` при каждом `edit_file`/`write_file`/`create_file`/`append_file`/`delete_file`/`move_file`; инструмент `show_file_history {path}` выводит список правок с датой и diff
- [x] **Оптимизация производительности IPC** — батчинг token/thinking-событий с 50ms flush; `setInterval` для runStats снижен с 1с до 3с; скрытый компонент `AllToolsGroup` убран из рендера (`return null`)
- [x] **Единый reset() для сброса стрима и очереди** — `onReset` callback добавлен в `useMessageQueue`; `resetQueue()` сам вызывает `onReset` (= `resetStreamState`); `useEffect([chatId])` в ChatPanel упрощён до одного вызова `resetQueue()`
- [x] **QueueStore (contexts/QueueContext)** — `chatBusy` вынесен в `QueueContext`; `ChatPanel` пишет в него через `setChatBusy`, `ChatHistoryPanel` читает без пропсов; `App.tsx` больше не держит `chatBusy` state
- [x] **ChatStore (contexts/ChatContext)** — `messages`, `activeChatId`, `chatStore`, `activeChat`, `activeProjectPath`, `interruptedDraft` вынесены в `ChatContext`; убрана пропс-цепочка App → ChatPanel → ChatHistoryPanel
- [x] **AgentStore (contexts/AgentContext)** — `agentPhase`, `runStats`, `generationMetrics`, `activeToolName`, `summarizing`, `runModel` вынесены из `useAgentStream` в `useReducer` + Context; `AgentStatusBar` и `App` читают из контекста без пропсов; `useAgentStream` принимает `dispatch` как параметр
- [x] **Вложения в чат** — кнопка «+» для выбора файлов; drag-and-drop; передача текстовых файлов в контекст (лимит 200 КБ); превью изображений в чате + base64 для мультимодальных моделей; Ctrl+V для вставки скриншотов; список файлов над полем ввода с именем, размером и кнопкой ✕; лимит 10 файлов за раз; суммарный счётчик
- [x] **Dual-provider режим (Ollama + облако одновременно)** — новые поля `cloudEnabled/cloudProvider/cloudApiKey/cloudModel` в `AgentSettings`; `buildSummarizeConfig()` в `AgentRunner` маршрутизирует суммаризацию контекста на второй провайдер; Ollama-агент суммаризирует через облако, облачный — через Ollama; UI-раздел «Облачный API (дополнительно)» в настройках
- [x] **Исправлен fallback на Ollama при облачном API** — убрана логика отката на Ollama при сбое cloud-провайдера; `modelRuntime.ts` напрямую делегирует в `this.provider.chat()`
- [x] **Отложенное применение правок агента** — если агент редактирует свои файлы вне режима самоулучшения, изменения уходят в `git stash`; при следующем запуске CodeViper.cmd предлагает применить их и пересобрать
- [x] **Параллельные агенты per-chat** — `agentRunState` и `activeAgentAbort` переведены на `Map<chatId, ...>`; каждый чат запускает и останавливает агента независимо; глобальная блокировка убрана
- [x] **Полное удаление ограничений по шагам и прогонам** — удалены `DEFAULT_MAX_STEPS`, `MAX_STEPS_MIN/MAX`, `DEFAULT_MAX_RUNS_PER_HOUR`, `MAX_RUNS_PER_HOUR_MIN/MAX` из констант; цикл агента `while (true)` с детектированием циклов через `MAX_CONSECUTIVE_SAME_TOOL`/`MAX_SAME_TOOL_TOTAL`
- [x] **Параллельное выполнение инструментов при облачных API** — при использовании DeepSeek/OpenAI все инструменты выполняются одновременно (Promise.all); N раундов → 1 раунд
- [x] **Отображение токенов и времени в топбаре** — чип «27s · 47.0k tok»; обновляется каждую секунду; Ollama — накопление `evalCount`; cloud — `sessionTokens`
- [x] **max_tokens и temperature для cloud-провайдеров** — `max_tokens: 4096`, `temperature: 0.1` при облачном провайдере
- [x] **Оптимизация токенов (промпт + инструменты)** — сокращён `BASE_SYSTEM_PROMPT`; descriptions инструментов сокращены вдвое
- [x] **Экспорт / импорт чатов** — кнопка `⇅`; экспорт JSON/Markdown; импорт с merge по id
- [x] **Группировка одинаковых инструментов в чате** — последовательные вызовы одного инструмента сворачиваются в один блок `⚙ toolName ✓ ×N ▼`
- [x] **Фикс 400 при самоулучшении (orphaned tool messages)** — двухпроходный фильтр orphaned tool/assistant пар
- [x] **Нативный tool calling для DeepSeek/OpenAI** — аккумуляция `delta.tool_calls`; `tool_call_id` в истории; полный multi-step tool calling по спецификации OpenAI
- [x] **Черновики при обрыве стрима** — `interruptedDraft` в SavedChat; баннер «Повторить» / «Закрыть»
- [x] **E2E-тесты (Playwright + Electron)** — startup, chat, ollama; CI через xvfb-run
- [x] **Нагрузочное тестирование** — 60 прогонов подряд; 10 параллельных чатов × 5 сообщений
- [x] **Агент не молчит при пустом ответе** — ошибка с советом если модель не вызвала инструменты
- [x] **Агент не останавливается на «намерении»** — повтор с обязательным tool call при отсутствии вызовов
- [x] **Не требовать инструменты для информационных вопросов** — `isInformationOnlyQuestion()` исключает вопросы из обязательного tool call
- [x] **Работа без Ollama при облачном провайдере** — `skipOllamaCheck` при выбранном DeepSeek/OpenAI
- [x] **Фикс масштабирования интерфейса** — `grid-template-columns: minmax(0, 1fr)`; `overflow-wrap: anywhere`
- [x] **Статус PR в UI** — панель PR; CI-статус; опрос каждые 60 с
- [x] **Изоляция стилей (вторая фаза)** — ModelPanel, TerminalPanel, MemoryPanel, SkillsPanel на CSS-модули
- [x] **Branch protection на master** — скрипт `docs/setup-branch-protection.ps1`
- [x] **Создание PR агентом** — инструмент `create_codeviper_pr`
- [x] **Расширенные настройки git-sync** — stash/rebase/ff-only; лаунчер читает стратегию из config.json
- [x] **Исправлены цвета акцентов тёмной темы** — hex-значения палитры GitHub-dark
- [x] **Выбор модели в хидере + динамический список DeepSeek** — пикер в топбаре; IPC `list-provider-models`
- [x] **Редизайн вкладки «Поведение»** — iOS-стиль переключатели; группировка по секциям
- [x] **Ветки для саморедактирования** — `create_codeviper_branch`, `push_codeviper_branch`
- [x] **Автовосстановление после краша** — `appState.json`; CrashRecoveryDialog
- [x] **Защита `parseToolArgs` + тесты агента** — try/catch с fallback; 37 тестов
- [x] **Документация API инструментов** — `docs/tools-api.md`; 42 инструмента
- [x] **Экспорт навыков и памяти** — GitHub Gist через API
- [x] **Настройка git-sync при старте** — тумблер; `start-dev.ps1` читает флаг
- [x] **Ужесточить дефолты безопасности** — `acceptEdits` по умолчанию; `sandbox: true`
- [x] **Санитизация путей в инструментах** — `assertInsideProject`; покрыто тестами
- [x] **Шифрование API-ключей** — `safeStorage.encryptString / decryptString`
- [x] **ESLint + Prettier** — `eslint.config.js`; `lint-staged` в husky
- [x] **Базовые возможности** — стриминг, кнопка «Стоп», выбор проекта, дерево файлов, настройки
- [x] **Инструменты агента** — create/edit/append/delete/move_file, git_*, grep, find; без shell-инъекций
- [x] **Управление моделями** — Ollama: каталог по RAM, автовыбор, скачивание/удаление; cloud API; think-блок
- [x] **История чатов** — папки, поиск, drag-and-drop, pin, теги, привязка к проекту
- [x] **Самообучение и память** — remember, рефлексия, план самоулучшения, anti-loop, эмбеддинги
- [x] **Надёжность агента** — парсинг text tool call, `tool_choice: required`, типизация, детектор опасных задач
- [x] **Надёжность очередей** — AgentError с кодами, таймаут, watchdog, retry с backoff, MAX_QUEUE_SIZE=50
- [x] **CI + pre-commit хуки** — husky + lint-staged; GitHub Actions
- [x] **Семантическое версионирование** — `bump-version.ts`; CHANGELOG.md; git-тег
- [x] **Изоляция стилей (первая фаза)** — ChatHistoryPanel, SettingsModal, ChatPanel на CSS-модули
- [x] **Worker для grep/find** — `grep_files` и `find_files` в `worker_threads`; прогресс через `postMessage`; main не блокируется
- [x] **Worker для эмбеддингов** — fetch к Ollama embed в `worker_thread`; singleton-воркер; LRU-кеш 500 записей; параллельные запросы без блокировки main
- [x] **Worker для парсинга больших файлов** — `readFile` + `split('\n')` + нарезка строк для файлов >512 КБ в `worker_thread`; singleton-воркер с очередью
- [x] **Открытие файлов из чата** — regex по тексту сообщений агента; клик открывает файл через `shell.showItemInFolder`; подсветка в inline-коде и обычном тексте
- [x] **Совместимость моделей** — ✓/⚠ значки рядом с каждой моделью; несовместимые по RAM отключены в `<select>`; причина показана в строке; данные из `getSystemCapabilities()`
- [x] **Адаптивные лимиты контекста** — `computeAdaptiveLimits` масштабирует `maxToolMessageChars` и `maxHistoryMessages` по окну контекста модели (16k→2k симв/12 сообщ, 128k→16k симв/80 сообщ); чип «Инструмент: Xk | История: N» в `AgentContextBar`
- [x] **Индикатор прогресса** — IPC `progress-event`; grep/find шлют процент
- [x] **Виртуализация длинных списков** — `@tanstack/react-virtual`
- [x] **Ленивая загрузка** — `React.lazy` для MessageBody, SettingsModal, TerminalPanel
- [x] **Автодополнение команд в терминале** — история 200 команд; выпадающий список
- [x] **Перехват ошибок и логирование** — `onerror`/`unhandledrejection`; `withTimeout`; frontend-log
- [x] **Суммаризация контекста** — сжатие при ~85%+ лимита; индикатор «Сжимаю контекст…»
- [x] **Метрики и мониторинг** — GPU/CPU-режим, tok/s, время шага; NDJSON-лог с ротацией
- [x] **UI/UX** — подсветка кода, ANSI-цвета, анимации, тема, горячие клавиши, звуковые уведомления
- [x] **Доступность** — фокус-trap, ARIA, `prefers-reduced-motion`, WCAG AA контрастность
- [x] **Безопасность** — `run_command` без `shell: true`; blocklist; кэш дерева файлов
- [x] **Инфраструктура** — рефакторинг agent.ts → 6 модулей; vitest; GitHub Actions CI
