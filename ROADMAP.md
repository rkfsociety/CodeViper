# Дорожная карта CodeViper

Планы развития и список выполненного. Назад в [README](README.md).

## 📋 В планах

Задачи сгруппированы по теме, внутри каждой группы — от лёгких к сложным. Нумерация начинается с 1.

---

### 🖱️ UI и удобство использования

- [ ] **1. Diff перед правками** — инструмент `preview_edit {path, content}` строит unified diff → stream-событие `preview`; UI показывает diff-блок с кнопками «Применить» / «Отмена»
- [ ] **2. История изменений файлов** — при каждом `edit_file`/`create_file` дописывать diff в NDJSON-лог рядом с agent-log; инструмент `show_file_history {path}` читает и выводит список правок

---

### 📎 Вложения в чат

- [ ] **3. Drag-and-drop файла в поле ввода** — слушать `dragover`/`drop` на `textarea`; `e.dataTransfer.files[0]`; показывать чип с именем файла и кнопкой «✕»; путь вставлять в текст сообщения автоматически при отправке
- [ ] **4. Кнопка «+» для выбора файла** — `<input type="file">` рядом с полем ввода; открывается через `dialog.showOpenDialog` (Electron); добавляет файл в список вложений
- [ ] **5. Передача текстовых файлов агенту** — при отправке читать содержимое вложенных файлов через `fs.readFile`; добавлять в контекст как `[filename]\n\`\`\`\n{content}\n\`\`\``; лимит — 200 КБ на файл
- [ ] **6. Превью изображений в чате** — если вложение — картинка (`png/jpg/gif/webp/svg`), показывать миниатюру в сообщении; в контекст передавать base64 для мультимодальных моделей (LLaVA, Qwen-VL и т.д.)
- [ ] **7. Вставка скриншота из буфера** — `Ctrl+V` в поле ввода перехватывает `clipboardData.items`; если там `image/*` — конвертировать в PNG через canvas, показывать превью, передавать в контекст base64
- [ ] **8. Многофайловые вложения** — список прикреплённых файлов над полем ввода; каждый с именем, размером и кнопкой удаления; лимит 10 файлов за раз; суммарный размер в чипе контекста

---

### 🔌 Провайдеры моделей

- [ ] **9. Провайдер Claude (Anthropic SDK)** — установить `@anthropic-ai/sdk`; адаптер `claudeProvider.ts` по образцу `openaiProvider.ts`; UI: пункт «Claude» в списке провайдеров + поле API key
- [ ] **10. Провайдер Gemini** — `@google/generative-ai`; адаптер `geminiProvider.ts`; маппинг tool calling в формат Gemini; UI аналогично п. 9
- [ ] **11. Провайдер Groq** — совместим с OpenAI API; переиспользовать `openaiProvider` с другим `baseUrl`; добавить в список провайдеров как отдельный пункт
- [ ] **12. Провайдер Together AI** — аналогично п. 11 (OpenAI-совместимый); отдельный пункт в UI

---

### 🏗️ Архитектура: единый стейт

- [ ] **13. Выделить AgentStore** — вынести `agentPhase`, `runStats`, `generationMetrics`, `activeToolName`, `summarizing` из `useAgentStream` в отдельный `useReducer` + Context; остальные компоненты читают из контекста без пропсов
- [ ] **14. Выделить ChatStore в Context** — `messages`, `draft`, `activeChatId` → один `ChatContext`; убрать цепочку пропсов через App → ChatPanel → дочерние
- [ ] **15. Выделить QueueStore** — логику очереди из `App.tsx` в отдельный хук `useAgentQueue`; единый источник правды для состояния очереди
- [ ] **16. Убрать дублирование resetStreamState / resetMessages** — после выделения сторов объединить функции сброса в один `reset()` из корневого контекста

---

### 💬 Параллельные чаты и проекты

- [ ] **17. Очередь сообщений per-chat** — `enqueuedMessages` вынести в Map по chatId; агенты в разных чатах не блокируют друг друга
- [ ] **18. UI: индикатор работы в других чатах** — иконка/пульс рядом с чатом в списке, если в нём сейчас работает агент
- [ ] **19. Каждый чат хранит свой projectPath** — `SavedChat.projectPath` уже есть; агент получает его из чата, а не из глобального состояния; UI показывает/переключает проект без перезагрузки

---

### 🔗 Внешние интеграции

- [ ] **20. Инструмент `trigger_github_workflow`** — `gh workflow run {workflow_id}`; просмотр статуса последних runs; GitHub token уже есть в настройках
- [ ] **21. Инструмент `create_issue`** — создание задач в GitHub Issues через `gh issue create`; поля: title, body, labels
- [ ] **22. Интеграция с Jira** — REST API: создать задачу `POST /rest/api/3/issue`; Jira URL и API token в настройках (новая вкладка)
- [ ] **23. Интеграция с Linear** — GraphQL API: мутация `issueCreate`; API key в настройках

---

### 🧠 Внешние базы знаний

- [ ] **24. Qdrant: настройки подключения** — вкладка в настройках: URL + API key; проверка соединения; `qdrant-client` в зависимостях
- [ ] **25. Индексирование проекта в Qdrant** — инструмент `index_project`: чанкует файлы, получает эмбеддинги, пишет в коллекцию; индикатор прогресса
- [ ] **26. Инструмент `search_knowledge_base`** — семантический поиск по проиндексированному проекту; возвращает top-N чанков с путями
- [ ] **27. Поддержка Milvus** — альтернативный адаптер; выбор провайдера базы знаний в настройках

---

### 🔌 MCP (Model Context Protocol)

- [ ] **28. Парсинг схем MCP-серверов** — читать `/.well-known/mcp` или JSON-схему по URL; преобразовывать `tools[]` в формат `AgentTool`
- [ ] **29. Регистрация MCP-инструментов в агенте** — динамически добавлять инструменты из MCP в список tools перед каждым прогоном
- [ ] **30. UI добавления MCP-серверов** — вкладка в настройках: список серверов, кнопка «+ Добавить по URL», статус подключения
- [ ] **31. Передача результатов обратно в MCP** — `POST /mcp/tool_result` после выполнения; поддержка stateful-сессий

---

### 🧩 Плагины

- [ ] **32. Загрузка плагинов из `~/.codeviper/plugins/`** — сканировать `*.ts`/`*.js` при старте; читать `export default { name, description, tools[] }`
- [ ] **33. Компиляция плагинов через esbuild** — запускать `esbuild` в main process; бандлить в CommonJS; кэшировать скомпилированный результат
- [ ] **34. Изолированное выполнение плагинов** — запускать в отдельном `worker_thread` с ограниченным API (`fs` только в projectPath, нет `net`)
- [ ] **35. UI управления плагинами** — вкладка в настройках: список, включить/выключить, просмотр инструментов плагина, кнопка «Открыть папку плагинов»

---

### 📦 Сборка и дистрибуция

- [ ] **36. CI → Release pipeline** — GitHub Actions: при push тега `v*` запускает `electron-builder --publish always` → GitHub Releases; артефакты: `.exe`, `.dmg`, `.AppImage`
- [ ] **37. `electron-updater`** — проверяет GitHub Releases при старте; скачивает в фоне; показывает баннер «Обновление готово» с кнопкой перезапуска
- [ ] **38. NSIS-установщик** — `electron-builder` + NSIS-скрипт; при установке клонирует репозиторий в `%APPDATA%\CodeViper\source\`; ярлык на рабочем столе
- [ ] **39. Portable Node.js в комплекте** — при сборке скачивать Node.js в `resources/node/`; использовать его для `npm run build` при самопересборке агента
- [ ] **40. Кроссплатформенность** — `.sh` launcher для Linux/macOS; CI-матрица `windows-latest` + `ubuntu-latest` + `macos-latest`; условные пути в main process
- [ ] **41. Docker-образ для разработки** — `Dockerfile` с Node.js + Ollama; `docker-compose.yml` с hot reload; инструкция в README

---

### 🌐 P2P-сеть вычислений

- [ ] **42. Сигнальный сервер** — VPS с реестром узлов (ID, GPU/RAM, нагрузка); Node.js + WebSocket + Redis; REST API для регистрации и опроса
- [ ] **43. Аккаунтная система** — регистрация по email/GitHub OAuth; JWT-токен определяет репутацию и лимиты
- [ ] **44. Лимит ресурсов узла** — `systeminformation`; при отдаче не более 20% GPU и 15% CPU; при превышении — пауза новых задач
- [ ] **45. Режим «Поделиться мощностью»** — переключатель в настройках; узел регистрируется на сервере и принимает входящие задачи
- [ ] **46. Очередь входящих задач** — не более 3 одновременно; приоритет по балансу кредитов; таймаут 60 с на задачу
- [ ] **47. Маршрутизация запросов** — сервер ищет свободный узел с нужной моделью; если нет — откат на локальную Ollama
- [ ] **48. Шифрование трафика** — TLS между узлами; end-to-end шифрование промптов; владелец узла не видит чужих данных
- [ ] **49. Система репутации** — кредиты за отданные вычисления; трата при использовании чужих; история в UI
- [ ] **50. Экран согласия** — явный диалог при первом включении режима P2P с объяснением что именно будет передаваться

---

## ✅ Сделано

- [x] **Параллельные агенты per-chat** — `agentRunState` и `activeAgentAbort` переведены на `Map<chatId, ...>`; каждый чат запускает и останавливает агента независимо; глобальная блокировка убрана
- [x] **Полное удаление ограничений по шагам и прогонам** — удалены `DEFAULT_MAX_STEPS`, `MAX_STEPS_MIN/MAX`, `DEFAULT_MAX_RUNS_PER_HOUR`, `MAX_RUNS_PER_HOUR_MIN/MAX` из констант; цикл агента `while (true)` с детектированием циклов через `MAX_CONSECUTIVE_SAME_TOOL`/`MAX_SAME_TOOL_TOTAL`
- [x] **Параллельное выполнение инструментов при облачных API** — при использовании DeepSeek/OpenAI все инструменты выполняются одновременно (Promise.all); N раундов → 1 раунд
- [x] **Отображение токенов и времени в топбаре** — чип «27s · 47.0k tok»; обновляется каждую секунду; Ollama — накопление `evalCount`; cloud — `sessionTokens`
- [x] **max_tokens и temperature для cloud-провайдеров** — `max_tokens: 4096`, `temperature: 0.1` при облачном провайдере
- [x] **Оптимизация токенов (промпт + инструменты)** — сокращён `BASE_SYSTEM_PROMPT`; descriptions инструментов сокращены вдвое
- [x] **Экспорт / импорт чатов** — кнопка `⇅`; экспорт JSON/Markdown; импорт с merge по id
- [x] **Группировка одинаковых инструментов в чате** — последовательные вызовы одного инструмента сворачиваются в один блок `⚙ toolName ✓ ×N ▼`
- [x] **Фикс 400 при самоулучшении (orphaned tool messages)** — двухпроходный фильтр orphaned tool/assistant пар
- [x] **Нативный tool calling для DeepSeek/OpenAI** — аккумуляция `delta.tool_calls`; `tool_call_id` в истории; полный multi-step tool calling по спецификации OpenAI
- [x] **Черновики при обрыве стрима** — `interruptedDraft` в SavedChat; баннер «Повторить» / «Закрыть»
- [x] **E2E-тесты (Playwright + Electron)** — startup, chat, ollama; CI через xvfb-run
- [x] **Нагрузочное тестирование** — 60 прогонов подряд; 10 параллельных чатов × 5 сообщений
- [x] **Агент не молчит при пустом ответе** — ошибка с советом если модель не вызвала инструменты
- [x] **Агент не останавливается на «намерении»** — повтор с обязательным tool call при отсутствии вызовов
- [x] **Не требовать инструменты для информационных вопросов** — `isInformationOnlyQuestion()` исключает вопросы из обязательного tool call
- [x] **Работа без Ollama при облачном провайдере** — `skipOllamaCheck` при выбранном DeepSeek/OpenAI
- [x] **Фикс масштабирования интерфейса** — `grid-template-columns: minmax(0, 1fr)`; `overflow-wrap: anywhere`
- [x] **Статус PR в UI** — панель PR; CI-статус; опрос каждые 60 с
- [x] **Изоляция стилей (вторая фаза)** — ModelPanel, TerminalPanel, MemoryPanel, SkillsPanel на CSS-модули
- [x] **Branch protection на master** — скрипт `docs/setup-branch-protection.ps1`
- [x] **Создание PR агентом** — инструмент `create_codeviper_pr`
- [x] **Расширенные настройки git-sync** — stash/rebase/ff-only; лаунчер читает стратегию из config.json
- [x] **Исправлены цвета акцентов тёмной темы** — hex-значения палитры GitHub-dark
- [x] **Выбор модели в хидере + динамический список DeepSeek** — пикер в топбаре; IPC `list-provider-models`
- [x] **Редизайн вкладки «Поведение»** — iOS-стиль переключатели; группировка по секциям
- [x] **Ветки для саморедактирования** — `create_codeviper_branch`, `push_codeviper_branch`
- [x] **Автовосстановление после краша** — `appState.json`; CrashRecoveryDialog
- [x] **Защита `parseToolArgs` + тесты агента** — try/catch с fallback; 37 тестов
- [x] **Документация API инструментов** — `docs/tools-api.md`; 42 инструмента
- [x] **Экспорт навыков и памяти** — GitHub Gist через API
- [x] **Настройка git-sync при старте** — тумблер; `start-dev.ps1` читает флаг
- [x] **Ужесточить дефолты безопасности** — `acceptEdits` по умолчанию; `sandbox: true`
- [x] **Санитизация путей в инструментах** — `assertInsideProject`; покрыто тестами
- [x] **Шифрование API-ключей** — `safeStorage.encryptString / decryptString`
- [x] **ESLint + Prettier** — `eslint.config.js`; `lint-staged` в husky
- [x] **Базовые возможности** — стриминг, кнопка «Стоп», выбор проекта, дерево файлов, настройки
- [x] **Инструменты агента** — create/edit/append/delete/move_file, git_*, grep, find; без shell-инъекций
- [x] **Управление моделями** — Ollama: каталог по RAM, автовыбор, скачивание/удаление; cloud API; think-блок
- [x] **История чатов** — папки, поиск, drag-and-drop, pin, теги, привязка к проекту
- [x] **Самообучение и память** — remember, рефлексия, план самоулучшения, anti-loop, эмбеддинги
- [x] **Надёжность агента** — парсинг text tool call, `tool_choice: required`, типизация, детектор опасных задач
- [x] **Надёжность очередей** — AgentError с кодами, таймаут, watchdog, retry с backoff, MAX_QUEUE_SIZE=50
- [x] **CI + pre-commit хуки** — husky + lint-staged; GitHub Actions
- [x] **Семантическое версионирование** — `bump-version.ts`; CHANGELOG.md; git-тег
- [x] **Изоляция стилей (первая фаза)** — ChatHistoryPanel, SettingsModal, ChatPanel на CSS-модули
- [x] **Worker для grep/find** — `grep_files` и `find_files` в `worker_threads`; прогресс через `postMessage`; main не блокируется
- [x] **Worker для эмбеддингов** — fetch к Ollama embed в `worker_thread`; singleton-воркер; LRU-кеш 500 записей; параллельные запросы без блокировки main
- [x] **Worker для парсинга больших файлов** — `readFile` + `split('\n')` + нарезка строк для файлов >512 КБ в `worker_thread`; singleton-воркер с очередью
- [x] **Открытие файлов из чата** — regex по тексту сообщений агента; клик открывает файл через `shell.showItemInFolder`; подсветка в inline-коде и обычном тексте
- [x] **Совместимость моделей** — ✓/⚠ значки рядом с каждой моделью; несовместимые по RAM отключены в `<select>`; причина показана в строке; данные из `getSystemCapabilities()`
- [x] **Адаптивные лимиты контекста** — `computeAdaptiveLimits` масштабирует `maxToolMessageChars` и `maxHistoryMessages` по окну контекста модели (16k→2k симв/12 сообщ, 128k→16k симв/80 сообщ); чип «Инструмент: Xk | История: N» в `AgentContextBar`
- [x] **Индикатор прогресса** — IPC `progress-event`; grep/find шлют процент
- [x] **Виртуализация длинных списков** — `@tanstack/react-virtual`
- [x] **Ленивая загрузка** — `React.lazy` для MessageBody, SettingsModal, TerminalPanel
- [x] **Автодополнение команд в терминале** — история 200 команд; выпадающий список
- [x] **Перехват ошибок и логирование** — `onerror`/`unhandledrejection`; `withTimeout`; frontend-log
- [x] **Суммаризация контекста** — сжатие при ~85%+ лимита; индикатор «Сжимаю контекст…»
- [x] **Метрики и мониторинг** — GPU/CPU-режим, tok/s, время шага; NDJSON-лог с ротацией
- [x] **UI/UX** — подсветка кода, ANSI-цвета, анимации, тема, горячие клавиши, звуковые уведомления
- [x] **Доступность** — фокус-trap, ARIA, `prefers-reduced-motion`, WCAG AA контрастность
- [x] **Безопасность** — `run_command` без `shell: true`; blocklist; кэш дерева файлов
- [x] **Инфраструктура** — рефакторинг agent.ts → 6 модулей; vitest; GitHub Actions CI
