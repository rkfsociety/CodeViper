# Дорожная карта CodeViper

Планы развития и список выполненного. Назад в [README](README.md).

## 📋 В планах

### Топ-10 приоритетов

- [ ] **Обработка ошибок** — класс `AgentError` и единый механизм обработки
- [ ] **Кэширование** — кэшировать файловое дерево на 5 секунд
- [ ] **Доступность** — ARIA-атрибуты, полноценное управление с клавиатуры
- [ ] **JSDoc** — документировать публичные API (`shared/`, IPC, инструменты)

### Производительность

- [ ] **Ленивая загрузка тяжёлых модулей** — `highlight.js`, `ReactMarkdown`, крупные библиотеки терминала грузить только по необходимости (при первом открытии сообщения с кодом или при запуске терминала) через динамический `import()` / `React.lazy` + `Suspense`
- [ ] **Инвалидация кэша файлового дерева** — кэш на 5–10 сек уже в планах, но дополнительно сбрасывать его при изменениях на диске (через `chokidar` или `fs.watch`)
- [ ] **Увеличить debounce `previewAgentContext`** — поднять задержку до 500–700 мс, чтобы не дёргать API на каждое нажатие клавиши
- [ ] **Виртуализация длинных списков** — история чатов, сообщения, память, навыки при большом числе элементов через `react-window` / `react-virtualized`
- [ ] **Мемоизация тяжёлых вычислений** — группировка чатов по папкам, фильтрация моделей, форматирование дат вынести в `useMemo`/`useCallback` с корректными зависимостями
- [ ] **Web Workers для тяжёлых операций** — парсинг больших файлов, поиск по коду, генерация эмбеддингов выгрузить из основного потока

### Архитектура и код

- [ ] **Тесты агента + защита parseToolArgs** — обернуть разбор аргументов tool call в `try/catch`: битый JSON у слабых моделей сейчас роняет весь прогон, нужно возвращать модели сообщение об ошибке для повтора; покрыть тестами: `executeTool` (моки fs/spawn), цикл самоулучшения, nudge при JSON-плане в тексте, `toolRequiresConfirm`; + интеграционный тест полного цикла агента

### Надёжность React-хуков (фронтенд)

Аудит хуков в `app/src/hooks/` выявил риски зависаний очереди, утечек и гонок.

**`useAgentStream.ts`:**

- [ ] **Замыкание на устаревшие `appendMessage`/`upsertMessage`** — `useEffect` с пустыми зависимостями захватывает функции из первого рендера; новые версии от родителя (после смены `onMessagesChange`) игнорируются. Использовать ref на актуальные коллбэки.
- [ ] **Очередь зависает при `error`** — событие `error` добавляет системное сообщение, но не вызывает `processNextQueuedRun`; нужно сбрасывать `agentRunning` и продолжать очередь.
- [ ] **Событие `done` может не прийти** — при ошибке/прерывании/зависании `agentRunning` не сбрасывается, очередь стоит. Нужен гарантированный финализатор (`finally`/watchdog).
- [ ] **Нет таймаута на ответ агента** — если модель/Ollama зависла, подписка висит вечно. Добавить таймаут с авто-сбросом состояния.
- [ ] **Дублирование сообщений при быстрых ответах** — гонка `token` → `assistant`: черновик не успевает очиститься, итоговое сообщение дублируется.
- [ ] **Незавершённые тулзы** — `tool_start` без `tool_end` оставляет сообщение в состоянии `start` навсегда; помечать как прерванное при завершении/ошибке прогона.

**`useContextPreview.ts`:**

- [ ] **Нет отмены запроса при размонтировании** — при смене чата/быстром вводе старый запрос завершается и пишет в размонтированный компонент. Добавить флаг отмены / `AbortController`.
- [ ] **Ошибки молча проглатываются** — в `catch` ставится `null` без сообщения; пользователь не узнаёт о сбое превью. Логировать и показывать.
- [ ] **Запрос при пустом вводе** — отправляется даже без текста; пропускать пустой ввод.

**`useMessageQueue.ts`:**

- [ ] **Очередь не продолжается после ошибок в `executeRun`** — при сбое `checkAgentPrerequisites`, отсутствии модели или ошибке `runAgent` `processNextQueuedRun` не вызывается; следующие сообщения зависают.
- [ ] **`stopAgent` не гарантирует продолжение очереди** — если `done` не пришёл, очередь стоит.
- [ ] **Гонка `stopAgent` ↔ `processNextQueuedRun`** — одновременное управление очередью → дубли/пропуски.
- [ ] **Нет ограничения длины очереди** — десятки сообщений подряд → разрастание и утечка памяти.
- [ ] **Нет повтора при временных сбоях** — упавший из-за сети `runAgent` не повторяется.
- [ ] **Рассинхрон `agentRunning`/`queueSize`** — одновременное использование ref и стейта → неверный статус.
- [ ] **При смене чата очередь не очищается полностью** — `resetQueue` сбрасывает очередь, но не останавливает текущий run; ответ может прийти в новый чат.

**`useOllamaDownloadQueue.ts`:**

- [ ] **Очередь останавливается при ошибке загрузки** — падение одной модели прерывает цикл, остальные не качаются.
- [ ] **Нет авто-повтора при сетевых сбоях** — пользователь должен вручную добавлять модель заново.
- [ ] **Нет таймаута на загрузку** — при «молчащем» сервере висит бесконечно.
- [ ] **Гонка при проверке установленных моделей** — `installedModels` не обновлён → дублирующая загрузка.
- [ ] **Ошибка `onRefresh` игнорируется** — модель удаляется из очереди даже если `onRefresh` упал.

**Общее для всех хуков:**

- [ ] **Нет глобального перехвата ошибок** — асинхронные ошибки проглатываются или уходят только в консоль, без уведомления пользователя.
- [ ] **Нет таймаутов для всех вызовов `window.codeviper.*`** — любой вызов может зависнуть навсегда.
- [ ] **Не везде отмена подписок/запросов при размонтировании** — особенно `useContextPreview`.
- [ ] **Нет логирования ошибок фронтенда в файл** — нет централизованного хранилища для диагностики у пользователя.
- [ ] **Нет проверки `chatId`/`projectPath` перед вызовами** — возможны вызовы с `null`/пустой строкой.
- [ ] **Стейт-менеджмент размазан по хукам** — фаза/модель/метрики агента дублируются в нескольких местах, сложно синхронизировать (кандидат на единый стор).

### Продукт и UX

- [ ] **Закрепление (pin) чатов** — важные чаты всегда сверху, даже при сортировке по дате
- [ ] **Теги для чатов** — метки (`bug`, `feature`, `refactor`) и фильтрация по ним
- [ ] **История изменений файлов** — diff до/после правок агента в чате или отдельной панели (частично есть через `git diff`)
- [ ] **Встроенный просмотрщик файлов** — открыть файл из чата в отдельной вкладке/панели без переключения в IDE
- [ ] **Автодополнение команд в терминале** — подсказки на основе истории и текущего проекта
- [ ] **Множественные проекты одновременно** — работа с несколькими проектами в разных чатах без переключения (сейчас один проект на чат)
- [ ] **Черновики ответов агента** — сохранять частичный ответ при обрыве стрима и возобновлять позже
- [ ] **Режим «Только чтение»** — агент анализирует код, но не вносит изменения (для code review)
- [ ] **Звуковые уведомления** — опционально, при завершении длительной задачи
- [ ] **Индикатор прогресса длительных операций** — расширить с скачивания моделей на `npm install`, `git clone`, большие операции поиска
- [ ] **Время выполнения запроса** — показывать `⏱ 2.3 сек` рядом с сообщением агента
- [ ] **Кнопка «Повторить»** для сообщений пользователя — повторно отправить тот же запрос
- [ ] **Кнопка «Редактировать»** для сообщений пользователя — изменить текст и отправить заново
- [ ] **Закрепление (pin) сообщений** в чате

### Установщик и саморедактирование через PR

Цель: установленное приложение умеет само себя улучшать — агент правит исходники,
открывает PR, ты его проверяешь и мержишь, приложение автоматически обновляется.

**Архитектура:** установщик кладёт бинарник (`CodeViper.exe`) и рядом клонирует
репозиторий в `source/`. Агент работает с `source/app/...` так же, как сейчас с
`F:\github\CodeViper\app\...`. После пересборки старый `.exe` заменяется новым.

- [ ] **NSIS-установщик** — `electron-builder` собирает `.exe`-установщик; при установке рядом с бинарником клонируется репозиторий (`git clone`) в подпапку `source/`; путь к исходникам сохраняется в настройках приложения
- [ ] **Node.js в комплекте** — установщик включает портативный Node.js (или проверяет системный); без него пересборка после правок невозможна
- [ ] **`gh` CLI в комплекте** — установщик включает портативный `gh` (GitHub CLI); агент использует его для создания веток и PR
- [ ] **Ветки для саморедактирования** — агент создаёт отдельную ветку `agent/fix-...` вместо прямого коммита в `master`; новые инструменты `create_codeviper_branch` + `push_codeviper_branch`
- [ ] **Создание PR** — после пуша агент вызывает `gh pr create` с описанием изменений; PR уходит на ревью к тебе, **не мержится автоматически**
- [ ] **Branch protection на `master`** — в настройках GitHub: запрет прямого пуша в `master`; обязательный CI (typecheck + tests) + ручной approve перед мержем
- [ ] **Статус PR в UI** — панель в интерфейсе со списком открытых PR агента: CI прошёл / упал, ожидает ревью; кнопка открыть PR на GitHub
- [ ] **Пересборка после мержа** — при запуске приложение проверяет, есть ли новые коммиты в `source/` (через `git fetch`); если да — запускает `npm run build` и предлагает перезапуститься с новым бинарником
- [ ] **CI → Release** — GitHub Actions: при мерже PR в `master` собирает новый `.exe` через `electron-builder --publish always` и публикует в GitHub Releases
- [ ] **electron-updater** — альтернативный путь обновления: `electron-updater` проверяет GitHub Releases при каждом запуске; скачивает новый `.exe` в фоне и предлагает перезапуск (работает параллельно с пересборкой из исходников)
- [ ] **Diff перед правками** — preview изменений в UI или инструмент `preview_edit` с подтверждением
- [ ] **Экспорт / импорт чатов** — JSON или Markdown, бэкап и восстановление
- [ ] **Параллельные чаты** — очередь или фоновый run по `chatId` (сейчас один глобальный `agentRunState`)

### Распределённая сеть узлов

Цель: пользователи по всему интернету объединяют мощности своих ПК — каждый отдаёт
до 20% GPU/CPU в общий пул и может пользоваться мощностями остальных.
Система работает по принципу «чем больше отдаёшь — тем больше получаешь».

**Инфраструктура (требует хостинг):**

- [ ] **Сигнальный сервер** — небольшой сервер (VPS) на хостинге; хранит реестр активных узлов (ID, характеристики GPU/RAM, текущая нагрузка, онлайн/офлайн); не обрабатывает запросы — только маршрутизирует соединения между клиентами; стек: Node.js + WebSocket + Redis
- [ ] **Аккаунтная система** — регистрация по email или GitHub OAuth; каждому пользователю выдаётся токен при установке; токен определяет репутацию и лимиты использования чужих ресурсов
- [ ] **Система репутации (баланс мощности)** — узел накапливает «кредиты» за отданные вычисления; тратит кредиты при использовании чужих узлов; нулевой баланс — только свой ПК; высокий баланс — доступ к мощным GPU в сети

**На каждом клиенте (в CodeViper):**

- [ ] **Режим «Поделиться мощностью»** — переключатель в настройках; при включении узел регистрируется на сигнальном сервере и начинает принимать входящие задачи
- [ ] **Лимит ресурсов** — монитор CPU/GPU (через `systeminformation` npm-пакет); жёсткое ограничение: не более 20% GPU и 15% CPU от текущей свободной мощности; при превышении — задача ставится на паузу
- [ ] **Очередь входящих задач** — не более 3 задач одновременно; приоритет отдаётся узлам с высоким балансом репутации; таймаут 60 сек — если не начал, задача уходит другому узлу
- [ ] **Маршрутизация исходящих запросов** — при запросе к модели CodeViper спрашивает сигнальный сервер: есть ли свободный узел с нужной моделью и достаточным балансом? Если да — отправляет туда; если нет — использует локальную Ollama
- [ ] **Шифрование трафика** — все промпты и ответы между узлами шифруются (TLS + end-to-end); владелец принимающего узла не видит содержимого чужих запросов в открытом виде
- [ ] **Согласие на участие** — при первом включении явный экран с предупреждением: чужие запросы обрабатываются на твоём ПК (зашифровано); пользователь явно принимает условия

### Агент и AI

- [ ] **MCP (Model Context Protocol)** — подключение внешних источников (БД, API, браузер) без своих инструментов под каждый случай
- [ ] **Поддержка других моделей** — помимо Ollama и OpenAI-совместимых: Claude (Anthropic), Gemini (Google), Mistral API, Groq, Together AI
- [ ] **Подключение внешних баз знаний** — векторные БД (Pinecone, Qdrant, Milvus) для семантического поиска по проектам
- [ ] **Плагины** — пользовательские инструменты и навыки на JS/TS, подключение без пересборки
- [ ] **Экспорт навыков/памяти** — поделиться навыками или памятью с другими (например, через GitHub Gist)
- [ ] **Интеграция с Jira / Linear / Trello** — агент создаёт задачи/тикеты по результатам анализа или багам
- [ ] **Интеграция с GitHub Actions** — запуск CI/CD пайплайнов и отображение их статуса

### Безопасность

- [ ] **Ужесточить безопасность по умолчанию** — поменять дефолт с `bypass` на `acceptEdits` + предупреждение при первом запуске; заменить `BLOCKED_PATTERNS` на whitelist безопасных команд (блок-лист обходится — реальная граница это `permissionMode`); добавить `sandbox: true` в `webPreferences`; запрет запуска `.ps1`/`.bat` без подтверждения
- [ ] **Настройка git-sync при старте** — вкл/выкл или предупреждение (сейчас `git stash` + `reset --hard` в `start-dev.ps1`)
- [ ] **Шифрование API-ключей и настроек** — хранить в `electron-store` с шифрованием через `safeStorage` из Electron
- [ ] **Валидация всех входных данных** — санитизация путей, команд, аргументов инструментов для защиты от инъекций (даже при наличии белого списка)
- [ ] **Ограничение прав доступа к ФС** — агент работает строго в пределах выбранного проекта (chroot-like), запрет чтения/записи за его границами
- [ ] **Подпись обновлений** — при использовании `electron-updater` проверять цифровую подпись релизов
- [ ] **Расширить подтверждение опасных операций** — `dangerDetector` дополнить операциями, влияющими на систему (изменение переменных окружения, глобальная установка пакетов)

### Инфраструктура

- [ ] **E2E-тесты** — smoke через Playwright для Electron (создать чат, ping Ollama)
- [ ] **Dev-окружение** — ESLint + Prettier (единый стиль, важно из-за саморедактирования агентом); hot reload main process (`restart_app` или авто-reload `electron/main/*` в dev)
- [ ] **Кроссплатформенность** — `.sh` launcher, тесты Linux/macOS в CI (сейчас Windows-first)
- [ ] **Нагрузочное тестирование** — поведение при большом числе одновременных запросов и длинных чатах
- [ ] **Тестирование на реальных проектах** — автопрогон агента на нескольких open-source проектах для выявления регрессий
- [ ] **Сбор и анализ метрик использования** — анонимные метрики (с согласия) о популярных функциях и местах ошибок
- [ ] **Автовосстановление после краша** — при падении main-процесса перезапуск с сохранением состояния чатов
- [ ] **Docker-образ для разработки** — чтобы не ставить Ollama и зависимости вручную
- [ ] **Линтинг и проверка типов в CI + pre-commit хуки** — дополнить ESLint + Prettier проверкой типов
- [ ] **Документация API инструментов** — чёткая спецификация для написания своих инструментов
- [ ] **Семантическое версионирование** — строгое `major.minor.patch` и автоматический changelog

### Быстрые победы

- [ ] **UI-полировка** — горячие клавиши (`Ctrl+,` настройки, `Ctrl+K` фокус ввода); переключатель светлой/тёмной темы в настройках; цветные иконки операций с файлами в чате (создание 🟢, правка 🟡, удаление 🔴, перемещение 🔵)
- [ ] **Анимации и микровзаимодействия** — `AgentStatusBar`: улучшенная пульсация, смена цветов по фазе; `ThinkingBlock`: анимация раскрытия (`max-height` + `transition`) и индикатор активности (три точки); сообщения: плавное появление (`@keyframes fadeIn`), hover-эффекты; `ChatPanel`: анимация скролла и переходы; современный спиннер загрузки; анимированные тосты/уведомления
- [ ] **Рефакторинг CSS (продолжение)** — удалить мёртвые классы (аудит: сравнить все классы в `styles.css` со всеми компонентами); исправить расхождение имён (`.context-modal` vs `.agent-context-modal`); проверить контрастность текста (особенно `var(--text-muted)`); добавить медиа-запросы для узких окон; убедиться что `disabled`-состояния есть у всех интерактивных элементов
- [ ] **Настраиваемый таймаут команд** — сейчас фиксированные 120 с
- [ ] **Откат последней правки** — snapshot перед `edit_file`
- [ ] **Индикатор GPU/CPU в статус-баре** — живой процент загрузки GPU и CPU модели во время генерации (через `systeminformation`); дополняет уже существующее предупреждение при CPU-режиме

**Приоритет внедрения:** константы → интеграционные тесты → установщик → diff в UI.

## ✅ Сделано

- [x] **Облачные API моделей** — поддержка DeepSeek и любых OpenAI-совместимых API: абстракция `ModelProvider`, `ModelRuntime`, унификация ответов в `ChatChunk`, UI выбора провайдера (URL, API-ключ с маскировкой, ping), `CloudModelSelector`, fallback на Ollama при ошибке облака, счётчик токенов сессии
- [x] **Глубокое рассуждение** — тумблер: think-моделям (qwen3, deepseek-r1, qwq) шлёт `think:true` (рассуждения видны сворачиваемым блоком «💭 Размышления»), остальным усиливает промпт
- [x] **Структурированное логирование** — каждый шаг агента, все tool call (имя, аргументы, время, ok/error), ответы LLM пишутся в NDJSON: `%APPDATA%/CodeViper/logs/agent-ГГГГ-ММ-ДД.ndjson`; файл ротируется раз в день
- [x] **Автокоммит самоправок** — после задачи, в которой агент менял свой код, он сам делает `git commit` + `push` (ограничено папкой `app/`)
- [x] **Синхронизация с GitHub при запуске** — `CodeViper.cmd` тянет свежую версию, локальные правки сохраняются в `git stash`
- [x] **Самообучение и память** — `remember`, рефлексия после задач, план `set_self_improvement_plan` + цикл, anti-loop (после 3 неудач пункт → `blocked`), семантический поиск (эмбеддинги `nomic-embed-text`)
- [x] **Суммаризация и мониторинг контекста** — сжатие при ~85%+ лимита, авто-выбор лёгкой модели (настраивается), панель «Контекст» (% лимита, секции, ~токены), индикатор «Сжимаю контекст…» в статус-баре
- [x] **Управление моделями Ollama** — автовыбор под задачу, каталог с группировкой по RAM (6 GB → 48 GB+), эскалация при рефьюзале, скачивание и удаление из UI
- [x] **История чатов** — папки, автосохранение, поиск, drag-and-drop, привязка проекта к папке, хранилище v2 (атомарная запись, отдельный файл на чат)
- [x] **GPU и метрики генерации** — `keep_alive` держит модель в VRAM, предупреждение при CPU-режиме, tok/s и время шага в статус-баре
- [x] **Инструменты файлов и Git** — `create/edit/append/delete/move_file` + `git_status/diff/log` (безопасные обёртки без shell-инъекций)
- [x] **Поиск в коде** — `grep_files`, `find_files`; 9 встроенных skills (agent-core, viper-skills, files, codebase…)
- [x] **CI/CD и тесты** — GitHub Actions (`typecheck` → `test` → `build`); vitest unit-тесты: chats, memory, services, parseToolArgs, shared/
- [x] **Режим уточняющих вопросов** — при неоднозначной задаче агент сначала задаёт вопросы, потом действует
- [x] **Режимы доступа и безопасность** — `Спрашивать всё` / `Принимать правки` / `Bypass`; `run_command` с blocklist, без `shell: true`
- [x] **Надёжность агента** — скрытие JSON tool call / парсинг текстовых `{"name": ...}`, проверка галлюцинаций (`tool_choice: required`), строгая типизация инструментов (`ToolName`/`ToolArgs`), константы в `shared/constants.ts`
- [x] **Рефакторинг кодовой базы** — ChatPanel разгружен (744→445 строк), agent.ts разбит на 6 доменных модулей
- [x] **Устойчивость** — таймауты Ollama (без зависаний), повреждённые хранилища сохраняются как `*.corrupt-<ts>`
- [x] **UI/UX** — визуальная полировка (переходы, тени, скроллбар), подсветка кода (highlight.js), clarifyMode-баннер, страница «О программе»
- [x] **Лимит прогонов в час** — настройка `maxRunsPerHour` (дефолт 20, диапазон 1–100) в настройках; скользящее окно 60 мин в `index.ts`; при превышении — сообщение об ошибке вместо запуска; защищает от перегрева Ollama и бесконечного цикла самоулучшения
- [x] **Детектор опасных задач** — анализ текста задачи до запуска (`shared/dangerDetector.ts`); паттерны: массовое удаление, `rm -rf`, опасные git-команды, DROP TABLE, форматирование диска и др.; при срабатывании — диалог с описанием опасности, агент не запускается без явного подтверждения
- [x] **CSS дизайн-система** — все жёстко заданные hex-цвета вынесены в CSS-переменные `:root`: 9 групп (`--bg-*`, `--border-*`, `--text-*`, `--blue-*`, `--green-*`, `--yellow-*`, `--red-*`, `--purple-*`, `--radius-*`); теперь смена акцентного цвета — правка одной строки
- [x] **Базовые возможности** — стриминг в реальном времени, кнопка «Стоп» с отменой fetch, проект выбирается внутри чата, путь проекта и дерево файлов в промпте, настройки в модальном окне, проверка зависимостей перед запуском
