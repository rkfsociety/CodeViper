# Дорожная карта CodeViper

Планы развития и список выполненного. Назад в [README](README.md).

## 📋 В планах

Задачи отсортированы от самых лёгких в реализации к самым сложным и пронумерованы
сквозной нумерацией. Уровни сложности: 🟢 мелкая правка 1–2 файла → 🟡 несколько
файлов без новых подсистем → 🟠 новая подсистема внутри приложения → 🔴 внешние
сервисы / инфраструктура / сборка → ⚫ распределённая инфраструктура.

### 🟢 Уровень 2а — очень лёгкие (1–2 файла)

- [ ] **31. Хуки: нет проверки `chatId`/`projectPath` перед вызовами** — добавить guard-условия в начало каждой async-функции, которая обращается к `window.codeviper.*`; предотвращает вызовы с `null`/пустой строкой
- [ ] **32. `useMessageQueue`: нет ограничения длины очереди** — добавить константу `MAX_QUEUE_SIZE = 50` и отбрасывать лишние сообщения с системным уведомлением; 3–5 строк в `submitMessage`
- [ ] **33. Расширить `dangerDetector`** — добавить паттерны: изменение переменных окружения (`export`, `set`, `.env` перезапись), глобальная установка пакетов (`npm install -g`, `pip install`, `choco install`), `curl | bash`
- [ ] **34. `useOllamaDownloadQueue`: ошибка `onRefresh` игнорируется** — обернуть вызов `onRefresh` в `try/catch`; при ошибке не удалять модель из очереди и показать сообщение
- [ ] **35. Хуки: не везде отмена подписок/запросов при размонтировании** — проверить все `useEffect` с `window.codeviper.*`; там, где нет cleanup, добавить флаг `let active = true` по образцу `useContextPreview`
- [ ] **36. `useMessageQueue`: рассинхрон `agentRunning`/`queueSize`** — заменить два параллельных источника истины (ref + useState) на один: `agentRunningRef` как единственный источник, `setAgentRunning` вызывается только рядом с его изменением
- [ ] **37. Инвалидация кэша файлового дерева по событиям ФС** — в `services.ts` добавить `fs.watch` на корень проекта; при любом событии вызвать `invalidateFileTreeCache(dirPath)`; убирает протухший кэш без перезапуска

### 🟡 Уровень 2б — несколько файлов, без новых подсистем

- [ ] **38. `useMessageQueue`: гонка `stopAgent` ↔ `processNextQueuedRun`** — добавить булев ref `stoppingRef`; `stopAgent` выставляет его перед `processNextQueuedRunRef.current()`, сбрасывает после; `processNextQueuedRun` пропускает следующий run если флаг выставлен
- [ ] **39. `useMessageQueue`: при смене чата очередь не очищается полностью** — в `resetQueue` добавить инкремент `runIdRef`, чтобы текущий run стал «устаревшим»; в `executeRun` и stream-обработчике проверять актуальность `runId` перед записью в стейт
- [ ] **40. `useOllamaDownloadQueue`: очередь останавливается при ошибке загрузки** — обернуть тело цикла в `try/catch`, при ошибке помечать модель как `error` и переходить к следующей
- [ ] **41. `useOllamaDownloadQueue`: нет авто-повтора при сетевых сбоях** — при ошибке загрузки делать до 3 повторов с экспоненциальной задержкой (1 с → 3 с → 9 с) перед отметкой `error`
- [ ] **42. `useOllamaDownloadQueue`: нет таймаута на загрузку** — добавить `AbortController` с таймаутом 10 мин; при превышении — ошибка с кодом `'timeout'` и переход к следующей модели
- [ ] **43. `useOllamaDownloadQueue`: гонка при проверке установленных моделей** — перед добавлением в очередь делать свежий fetch `installedModels` (или хранить Set уже-добавленных в течение сессии) вместо чтения устаревшего стейта
- [ ] **44. Хуки: нет глобального перехвата ошибок** — подписаться на `window.onerror` и `window.addEventListener('unhandledrejection', ...)` в `App.tsx`; показывать системное сообщение в активный чат или toast-уведомление
- [ ] **45. Хуки: нет таймаутов для всех вызовов `window.codeviper.*`** — создать хелпер `withTimeout<T>(promise, ms, label)` в `shared/`; обернуть все вызовы кроме стриминга (`runAgent` уже защищён); дефолт 30 с
- [ ] **46. Хуки: нет логирования ошибок фронтенда в файл** — добавить IPC-канал `log-frontend-error`; в рендерере перехватывать ошибки и отправлять через preload; в main — аппендить в `logs/frontend-YYYY-MM-DD.ndjson`
- [ ] **47. Валидация входных данных / санитизация путей** — в `agentHandlersProject.ts` проверять, что пути аргументов (`path`, `from`, `to`) остаются внутри `projectPath` после `path.resolve`; некорректные — `AgentError('readonly')`
- [ ] **48. `useMessageQueue`: нет повтора при временных сбоях сети** — в catch `executeRun` различать сетевые ошибки (`AgentError('stream_lost')`, `fetch failed`) и делать 1 автоповтор через 2 с; остальные ошибки — сразу в очередь
- [ ] **49. Ограничение прав доступа к ФС** — расширить guard из п. 47: запрет не только записи, но и чтения (`read_file`, `grep_files`, `find_files`) за пределами `projectPath`; добавить вспомогательную `assertInsideProject(path, projectPath)`
- [ ] **50. Шифрование API-ключей и настроек** — заменить хранение `apiKey`/`ollamaUrl` в plain JSON на `safeStorage.encryptString` / `decryptString` из Electron; прозрачно для UI через IPC
- [ ] **51. Dev-окружение: ESLint + Prettier** — добавить `.eslintrc.cjs` (typescript-eslint + react-hooks), `.prettierrc`; скрипты `lint` и `format` в `package.json`; важно для саморедактирования агентом
- [ ] **52. Семантическое версионирование + changelog** — добавить скрипт `scripts/bump-version.ts` (patch/minor/major); автогенерация записи в `CHANGELOG.md` из git log с момента последнего тега; вызывается вручную или после PR-мержа

### 🟠 Уровень 3 — новые подсистемы внутри приложения

- [ ] **53. Защита `parseToolArgs` + тесты агента** — обернуть `JSON.parse` в `parseToolArgs` в `try/catch` с fallback на пустой объект и логирование; покрыть тестами: `executeTool` (моки fs/spawn), `toolRequiresConfirm`, nudge при JSON-плане, цикл самоулучшения; + интеграционный тест полного прогона
- [ ] **54. Ленивая загрузка тяжёлых модулей** — `highlight.js`, `ReactMarkdown`, компоненты терминала переключить на `React.lazy` + `Suspense` с динамическим `import()`; ускоряет холодный старт
- [ ] **55. Виртуализация длинных списков** — заменить «Показать ещё» на `@tanstack/virtual` для истории чатов, списка памяти и навыков; необходимо при тысячах записей
- [ ] **56. Индикатор GPU/CPU в статус-баре** — интегрировать `systeminformation` в main-процесс; IPC-опрос каждые 2 с во время генерации; отображать `GPU X% / CPU Y%` рядом с tok/s
- [ ] **57. Индикатор прогресса длительных операций** — выделить общий IPC-канал `progress-event {label, percent}`; подключить к `run_command` (npm install, git clone) и поиску по коду; отображать в статус-баре
- [ ] **58. Экспорт / импорт чатов** — кнопка в ChatHistoryPanel: экспорт выбранных чатов в JSON или Markdown; импорт из файла с проверкой схемы и merge по `id`
- [ ] **59. История изменений файлов агентом** — в `edit_file`/`create_file` записывать diff (`before`/`after`) в отдельный NDJSON-файл рядом с логом агента; новый инструмент `show_file_history` выводит список правок для файла
- [ ] **60. Diff перед правками (preview_edit)** — новый инструмент `preview_edit {path, content}`: вычисляет unified diff, отправляет stream-событием `preview`; UI показывает diff-блок с кнопками «Применить» / «Отмена»
- [ ] **61. Встроенный просмотрщик файлов** — по клику на путь в сообщении чата открывать панель с содержимым файла (read_file через IPC) с подсветкой синтаксиса; вкладка рядом с деревом файлов
- [ ] **62. Черновики при обрыве стрима** — при `stream_lost` / timeout сохранять накопленный `draft` в `SavedChat.interruptedDraft`; при следующем открытии чата предлагать продолжить или удалить черновик
- [ ] **63. Настройка git-sync при старте** — тумблер в настройках «Синхронизировать при запуске»; `start-dev.ps1` читает флаг из `config.json`; при выключенном — пропускает `stash`/`reset`
- [ ] **64. Ужесточить безопасность по умолчанию** — переключить дефолт `bypass` → `acceptEdits`; добавить предупреждение при первом запуске; `sandbox: true` в `webPreferences`; запрос подтверждения для `.ps1`/`.bat`
- [ ] **65. Документация API инструментов** — сгенерировать `docs/tools-api.md` из схем `AGENT_TOOLS`; описание аргументов, примеры, когда использовать; нужна для авторов плагинов
- [ ] **66. Экспорт навыков и памяти** — кнопка «Поделиться» в панели навыков/памяти: создаёт GitHub Gist через API (токен из настроек) и копирует ссылку в буфер
- [ ] **67. Изоляция стилей компонентов** — поэтапный перевод `styles.css` (>2000 строк) на CSS-модули (`*.module.css`): начать с ChatPanel, ChatHistoryPanel, SettingsModal; остальное в следующих итерациях
- [ ] **68. CI: проверка типов + pre-commit хуки** — в GitHub Actions добавить шаг `tsc --noEmit`; локально — `husky` + `lint-staged` для `eslint --fix` и `prettier --write` перед коммитом
- [ ] **69. Автодополнение команд в терминале** — хранить последние 200 выполненных команд в `localStorage`; при вводе в поле сообщения (после `/`) показывать выпадающий список совпадений

### 🔴 Уровень 4 — сложные подсистемы и инфраструктура

- [ ] **70. Параллельные чаты** — перенести `agentRunState` из глобального singleton в Map по `chatId`; каждый чат имеет свою очередь и поток; требует рефактора IPC-каналов и хуков
- [ ] **71. Множественные проекты одновременно** — UI выбора проекта переключается без перезагрузки; каждый чат хранит свой `projectPath`; агент получает правильный путь по `chatId`
- [ ] **72. Единый стейт-менеджмент** — собрать фазу агента, модель, метрики, draft, очередь в один стор (Zustand или `useReducer` + Context); убрать дублирование между хуками
- [ ] **73. Поддержка дополнительных провайдеров моделей** — Claude (Anthropic SDK), Gemini (Google AI), Groq, Together AI; абстракция уже есть (`ModelProvider`), нужны конкретные адаптеры и UI выбора ключей
- [ ] **74. Web Workers для тяжёлых операций** — парсинг больших файлов, grep по всему проекту, вычисление эмбеддингов вынести в Worker через `worker_threads` (main process) или `new Worker` (renderer)
- [ ] **75. MCP (Model Context Protocol)** — поддержка MCP-серверов как источников инструментов; UI для добавления серверов по URL; динамическая регистрация `AGENT_TOOLS` из схем MCP
- [ ] **76. Плагины** — система загрузки пользовательских инструментов из `~/.codeviper/plugins/*.ts`; компиляция через `esbuild` в runtime; изолированное выполнение с ограниченным API
- [ ] **77. Подключение внешних баз знаний** — интеграция с Qdrant/Milvus через REST API; инструмент `search_knowledge_base`; UI настройки подключения; используется для семантического поиска по большим кодовым базам
- [ ] **78. E2E-тесты** — smoke-тесты через Playwright для Electron: создать чат, ввести сообщение, проверить ответ, ping Ollama; запуск в CI перед release
- [ ] **79. Автовосстановление после краша** — сохранять `appState.json` каждые 30 с (активный чат, проект, очередь); при следующем запуске предлагать восстановление
- [ ] **80. Настройка git-sync при старте (расширенная)** — вкл/выкл через UI; дополнительно: выбор стратегии (stash / rebase / pull --ff-only); предупреждение если есть uncommitted изменения
- [ ] **81. Ветки для саморедактирования агента** — инструменты `create_codeviper_branch {name}` и `push_codeviper_branch`; агент создаёт `agent/fix-...` вместо коммита в `master`
- [ ] **82. Создание PR агентом** — после push агент вызывает `gh pr create` с описанием изменений; PR не мержится автоматически — только после ревью пользователя
- [ ] **83. Branch protection на `master`** — настройка через `gh api`: запрет прямого push, обязательный CI, ручной approve; инструкция и скрипт в `docs/`
- [ ] **84. Статус PR в UI** — панель в сайдбаре: список открытых PR агента из `gh pr list`, статус CI, кнопка «Открыть на GitHub»; опрос каждые 60 с
- [ ] **85. Пересборка после мержа PR** — при запуске `git fetch`; если `source/` обновился — `npm run build`; уведомление с кнопкой «Перезапустить»
- [ ] **86. NSIS-установщик** — `electron-builder` + NSIS: собирает `.exe`; при установке клонирует репозиторий в `%APPDATA%\CodeViper\source\`; путь сохраняется в `config.json`
- [ ] **87. Portable Node.js в комплекте** — скачивать portable Node.js в `resources/node/` при сборке; использовать для `npm run build` при пересборке после PR
- [ ] **88. CI → Release** — GitHub Actions: при push тега `v*` собирает `.exe` через `electron-builder --publish always` и публикует в GitHub Releases
- [ ] **89. `electron-updater`** — проверяет GitHub Releases при запуске; качает обновление в фоне; предлагает перезапуск; проверяет подпись
- [ ] **90. Кроссплатформенность** — `.sh` launcher для Linux/macOS; CI матрица `windows-latest` + `ubuntu-latest` + `macos-latest`; условные пути в main process
- [ ] **91. Docker-образ для разработки** — `Dockerfile` с Node.js + Ollama + зависимостями; `docker-compose.yml` с hot reload; облегчает онбординг
- [ ] **92. Нагрузочное тестирование** — скрипт: 50+ сообщений подряд, параллельные чаты, длинная история; измерение памяти рендерера и main process
- [ ] **93. Сбор анонимных метрик** — экран согласия при первом запуске; сбор: версия, провайдер модели, частота инструментов, ошибки; отправка в собственный endpoint (без PII)
- [ ] **94. Интеграция с GitHub Actions** — инструмент `trigger_workflow {repo, workflow_id}`; просмотр статуса runs; требует GitHub token в настройках
- [ ] **95. Интеграция с Jira / Linear** — инструмент `create_issue {title, body, project}`; через REST API с токеном; UI настройки подключения

### ⚫ Уровень 5 — распределённая сеть узлов (хостинг, P2P, аккаунты)

Цель: пользователи объединяют мощности ПК — каждый отдаёт до 20% GPU/CPU в общий
пул и пользуется мощностями остальных по принципу «чем больше отдаёшь — тем больше
получаешь».

- [ ] **96. Сигнальный сервер** — VPS с реестром активных узлов (ID, GPU/RAM, нагрузка, онлайн); маршрутизирует соединения между клиентами; Node.js + WebSocket + Redis
- [ ] **97. Аккаунтная система** — регистрация по email/GitHub OAuth; токен при установке определяет репутацию и лимиты
- [ ] **98. Система репутации (баланс мощности)** — узел копит «кредиты» за отданные вычисления, тратит при использовании чужих; нулевой баланс — только свой ПК
- [ ] **99. Режим «Поделиться мощностью»** — переключатель; узел регистрируется на сервере и принимает входящие задачи
- [ ] **100. Лимит ресурсов** — монитор CPU/GPU (`systeminformation`); не более 20% GPU и 15% CPU свободной мощности; при превышении — пауза
- [ ] **101. Очередь входящих задач** — не более 3 одновременно; приоритет узлам с высоким балансом; таймаут 60 сек
- [ ] **102. Маршрутизация исходящих запросов** — сервер ищет свободный узел с нужной моделью и достаточным балансом; иначе локальная Ollama
- [ ] **103. Шифрование трафика** — промпты и ответы между узлами (TLS + end-to-end); владелец узла не видит содержимого чужих запросов
- [ ] **104. Согласие на участие** — явный экран при первом включении: чужие запросы обрабатываются на твоём ПК (зашифровано)

## ✅ Сделано

### 🟡 Уровень 2 — надёжность и устойчивость

- [x] **20. Обработка ошибок** — создан `shared/agentError.ts`: класс `AgentError` с кодами `timeout | no_model | prerequisites | run_failed | stream_lost | readonly`
- [x] **21. Кэширование файлового дерева** — Map-кэш с TTL 8 с для `buildFileTree` в `services.ts`; `invalidateFileTreeCache()` для ручного сброса
- [x] **22. Доступность (расширить)** — `aria-label` на всех icon-only кнопках в ChatHistoryPanel и ChatPanel; `role="tree"` на списке чатов; `aria-pressed` для toggle-кнопок
- [x] **23. `useAgentStream`: замыкание на устаревшие коллбэки** — `appendMessage`, `upsertMessage`, `setContextPreview` обёрнуты в рефы; эффект всегда видит актуальные функции
- [x] **24. `useAgentStream`: очередь зависает при `error`** — событие `error` теперь тоже вызывает `processNextQueuedRunRef` (с guard через `doneRunIdRef`)
- [x] **25. `useAgentStream`: событие `done` может не прийти** — watchdog: 150 мс после `runAgent` resolve, затем проверка `doneRunIdRef` и принудительный `processNextQueuedRun`
- [x] **26. `useAgentStream`: нет таймаута на ответ агента** — `Promise.race` с `AgentError('timeout')` через `AGENT_RUN_TIMEOUT_MS` (10 мин)
- [x] **27. `useAgentStream`: дублирование сообщений при быстрых ответах** — `visibleDraft` подавляется, если последнее assistant-сообщение имеет идентичный контент
- [x] **28. `useAgentStream`: незавершённые инструменты** — в обработчике `done`: если `activeToolMessageIdRef` не пуст — `upsertMessage` с `⚠️ Прервано`
- [x] **29. `useMessageQueue`: очередь не продолжается после ошибок** — catch в `executeRun` вызывает `processNextQueuedRunRef`; вся ручная очистка стейта убрана
- [x] **30. `useMessageQueue`: `stopAgent` не гарантирует продолжение очереди** — 250 мс задержка после `stopAgent()`, затем принудительный `processNextQueuedRun` если агент всё ещё числится запущенным

### 🟢 Уровень 1 — мелкие правки в 1–2 файлах

- [x] **1. Увеличить debounce `previewAgentContext`** — поднять задержку до 500–700 мс, чтобы не дёргать API на каждое нажатие клавиши
- [x] **2. Настраиваемый таймаут команд** — сейчас фиксированные 120 с, вынести в настройки
- [x] **3. Мемоизация тяжёлых вычислений** — группировка чатов по папкам, фильтрация моделей, форматирование дат вынести в `useMemo`/`useCallback` с корректными зависимостями
- [x] **4. `useContextPreview`: запрос при пустом вводе** — отправляется даже без текста; пропускать пустой ввод
- [x] **5. `useContextPreview`: ошибки молча проглатываются** — в `catch` ставится `null` без сообщения; логировать и показывать
- [x] **6. `useContextPreview`: нет отмены запроса при размонтировании** — при смене чата/быстром вводе старый запрос пишет в размонтированный компонент; добавить флаг `active`
- [x] **7. Рефакторинг CSS (остаток)** — добавлен `.chat-history-item-main {flex:1; min-width:0}`; `.context-modal` в порядке
- [x] **8. Анимации (остаток)** — `ThinkingBlock.live`: анимация `thinking-pulse` при стриминге
- [x] **9. Время выполнения запроса** — `durationMs` в `ChatMessage`; `⏱ X.Xs` рядом с сообщением агента
- [x] **10. Кнопка «Повторить»** для сообщений пользователя — повторно отправить тот же запрос
- [x] **11. Кнопка «Редактировать»** для сообщений пользователя — изменить текст и отправить заново
- [x] **12. Закрепление (pin) чатов** — `pinned` в `SavedChat`; закреплённые всегда сверху; кнопка 📌 в боковой панели
- [x] **13. Закрепление (pin) сообщений** в чате — локальное состояние `pinnedMessageIds`; секция «📌 Закреплённые» вверху
- [x] **14. Теги для чатов** — `tags?: string[]` в `SavedChat`; фильтрация по клику на тег; поле tagFilter в поиске
- [x] **15. Режим «Только чтение»** — `readonlyMode` в настройках; блокирует write-инструменты агента
- [x] **16. Звуковые уведомления** — `soundNotifications` в настройках; короткий beep через Web Audio API при завершении задачи
- [x] **17. Откат последней правки** — snapshot перед `edit_file`; инструмент `undo_edit` для агента
- [x] **18. JSDoc** — добавлены описания констант в `shared/constants.ts`
- [x] **19. UI-полировка** — `Ctrl+,` открывает настройки; `Ctrl+K` фокус ввода; кнопка ☀️/🌙 переключения темы

### Более ранние задачи

- [x] **Адаптивность и доступность (a11y)** — медиа-запросы для планшетов/мобильных/очень узких экранов (сайдбар сворачивается над чатом, модалки на всю ширину); фокус-trap в модалках (`useModalA11y`: перевод фокуса внутрь, возврат на триггер, Tab-цикл, `aria-modal`/`aria-describedby`); поддержка `prefers-reduced-motion`; контрастность вторичного текста повышена до WCAG AA (`--text-muted` светлее)
- [x] **Анимации и микровзаимодействия** — fade-in новых сообщений (`@keyframes message-fadein` со смещением вверх); плавное раскрытие `ThinkingBlock` через `grid-template-rows 0fr→1fr` (state-driven вместо нативного `<details>`); пульсация статус-бара на `opacity`+`transform` через `::after` (GPU-friendly)
- [x] **ANSI-цвета в терминале** — `shared/ansi.ts` парсит SGR-коды (цвета, bold, italic, underline, 256/truecolor) вывода команд в стилизованные сегменты; покрыт тестами
- [x] **Вкладки настроек и единые диалоги** — `SettingsModal` разбит на вкладки (Модель / Поведение / Память и навыки); нативный `window.confirm` в `ModelPanel` заменён на стилизованный `ConfirmDialog`; пагинация «Показать ещё» для истории чатов, памяти и навыков; яркий отклик зоны drop-target
- [x] **Облачные API моделей** — поддержка DeepSeek и любых OpenAI-совместимых API: абстракция `ModelProvider`, `ModelRuntime`, унификация ответов в `ChatChunk`, UI выбора провайдера (URL, API-ключ с маскировкой, ping), `CloudModelSelector`, fallback на Ollama при ошибке облака, счётчик токенов сессии
- [x] **Глубокое рассуждение** — тумблер: think-моделям (qwen3, deepseek-r1, qwq) шлёт `think:true` (рассуждения видны сворачиваемым блоком «💭 Размышления»), остальным усиливает промпт
- [x] **Структурированное логирование** — каждый шаг агента, все tool call (имя, аргументы, время, ok/error), ответы LLM пишутся в NDJSON: `%APPDATA%/CodeViper/logs/agent-ГГГГ-ММ-ДД.ndjson`; файл ротируется раз в день
- [x] **Автокоммит самоправок** — после задачи, в которой агент менял свой код, он сам делает `git commit` + `push` (ограничено папкой `app/`)
- [x] **Синхронизация с GitHub при запуске** — `CodeViper.cmd` тянет свежую версию, локальные правки сохраняются в `git stash`
- [x] **Самообучение и память** — `remember`, рефлексия после задач, план `set_self_improvement_plan` + цикл, anti-loop (после 3 неудач пункт → `blocked`), семантический поиск (эмбеддинги `nomic-embed-text`)
- [x] **Суммаризация и мониторинг контекста** — сжатие при ~85%+ лимита, авто-выбор лёгкой модели (настраивается), панель «Контекст» (% лимита, секции, ~токены), индикатор «Сжимаю контекст…» в статус-баре
- [x] **Управление моделями Ollama** — автовыбор под задачу, каталог с группировкой по RAM (6 GB → 48 GB+), эскалация при рефьюзале, скачивание и удаление из UI
- [x] **История чатов** — папки, автосохранение, поиск, drag-and-drop, привязка проекта к папке, хранилище v2 (атомарная запись, отдельный файл на чат)
- [x] **GPU и метрики генерации** — `keep_alive` держит модель в VRAM, предупреждение при CPU-режиме, tok/s и время шага в статус-баре
- [x] **Инструменты файлов и Git** — `create/edit/append/delete/move_file` + `git_status/diff/log` (безопасные обёртки без shell-инъекций)
- [x] **Поиск в коде** — `grep_files`, `find_files`; 9 встроенных skills (agent-core, viper-skills, files, codebase…)
- [x] **CI/CD и тесты** — GitHub Actions (`typecheck` → `test` → `build`); vitest unit-тесты: chats, memory, services, parseToolArgs, shared/
- [x] **Режим уточняющих вопросов** — при неоднозначной задаче агент сначала задаёт вопросы, потом действует
- [x] **Режимы доступа и безопасность** — `Спрашивать всё` / `Принимать правки` / `Bypass`; `run_command` с blocklist, без `shell: true`
- [x] **Надёжность агента** — скрытие JSON tool call / парсинг текстовых `{"name": ...}`, проверка галлюцинаций (`tool_choice: required`), строгая типизация инструментов (`ToolName`/`ToolArgs`), константы в `shared/constants.ts`
- [x] **Рефакторинг кодовой базы** — ChatPanel разгружен (744→445 строк), agent.ts разбит на 6 доменных модулей
- [x] **Устойчивость** — таймауты Ollama (без зависаний), повреждённые хранилища сохраняются как `*.corrupt-<ts>`
- [x] **UI/UX** — визуальная полировка (переходы, тени, скроллбар), подсветка кода (highlight.js), clarifyMode-баннер, страница «О программе»
- [x] **Лимит прогонов в час** — настройка `maxRunsPerHour` (дефолт 20, диапазон 1–100) в настройках; скользящее окно 60 мин в `index.ts`; при превышении — сообщение об ошибке вместо запуска; защищает от перегрева Ollama и бесконечного цикла самоулучшения
- [x] **Детектор опасных задач** — анализ текста задачи до запуска (`shared/dangerDetector.ts`); паттерны: массовое удаление, `rm -rf`, опасные git-команды, DROP TABLE, форматирование диска и др.; при срабатывании — диалог с описанием опасности, агент не запускается без явного подтверждения
- [x] **CSS дизайн-система** — все жёстко заданные hex-цвета вынесены в CSS-переменные `:root`: 9 групп (`--bg-*`, `--border-*`, `--text-*`, `--blue-*`, `--green-*`, `--yellow-*`, `--red-*`, `--purple-*`, `--radius-*`); теперь смена акцентного цвета — правка одной строки
- [x] **Базовые возможности** — стриминг в реальном времени, кнопка «Стоп» с отменой fetch, проект выбирается внутри чата, путь проекта и дерево файлов в промпте, настройки в модальном окне, проверка зависимостей перед запуском
