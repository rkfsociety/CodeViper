# Дорожная карта CodeViper

Планы развития и список выполненного. Назад в [README](README.md).

## 📋 В планах

Задачи отсортированы по сложности выполнения агентом: от однофайловых правок до отдельных инфраструктурных подсистем.

---

### 🟢 Лёгкие — однофайловые, < 2 часа

- [ ] **UI: индикатор работы в других чатах** — иконка/пульс рядом с чатом в списке, если в нём сейчас работает агент; время: 30 мин
- [ ] **Поддержка альтернативных моделей оркестратора** — кнопка «Выбрать свою модель» → `dialog.showOpenDialog` с фильтром `.gguf`; путь сохраняется в `config.json`; время: 1-1.5 ч

---

### 🟡 Средние — несколько файлов, полдня–день

**Оптимизация токенов (из TokenJam анализа: 70-95% экономия)**

Рекомендации по результатам анализа телеметрии агента. Текущая затрата: 2,131 LLM запрос/день, 5.2 МБ выводов инструментов. После реализации: сессия 100k → 10-30k токенов.

- [ ] **LRU-кэш для read_file/read_codeviper_file** — кэш 500 записей по ключу `{path, offset?, limit?}` в `agentContext.ts`; инвалидация по `mtime` файла; 4.4 МБ → 2.6 МБ (40% экономия, 250k токенов/день); аналог существующего LRU для эмбеддингов; время: 2-3 ч
- [ ] **Удаление старых результатов инструментов из контекста** — оставлять результаты только последних K вызовов (дефолт 5), более старые → `[результат обрезан]` в `compressContextMessages()`; экономия 10–15% на длинных диалогах (30+ шагов, ~100k токенов); время: 1 ч
- [ ] **Батчинг запросов grep** — группировать параллельные grep вызовы (до 4-5 паттернов за раз) в `fileSearch.ts`; 800 вызовов → 200-300; экономия 15–20% (210 КБ → 170 КБ, ~100k токенов); отдельно от embeddings батчинга; время: 1-2 ч
- [ ] **Автообрезка больших файлов** — файлы > 20 КБ читать как первые + последние 50 строк в `agentContext.ts`; маркер `... (X строк обрезано) ...` посередине; экономия 5–10% на сессиях с крупными файлами; время: 1.5 ч
- [ ] **Кэш результатов grep/find** — по ключу `{query, path, glob}` в `fileSearch.ts`; инвалидация по `mtime` файлов; экономия 5–8% (50-80k токенов на активных сессиях); время: 1 ч
- [ ] **Дедупликация результатов инструментов** — если `tool_result` идентичен предыдущему, слить в один с пометкой `(повторено N раз)` в `compressContextMessages()`; экономия 10–20% на длинных поисках; время: 1 ч
- [ ] **Удаление результатов ошибок старше N попыток** — из контекста удалять tool results с ошибками (начинаются с `Ошибка:`) старше последней; оставлять только последнюю попытку; экономия 5–10%; время: 30 мин

**Архитектура и интеграции**

- [ ] **Retry + backoff в selfCommit.ts** — обернуть git-операции в retry с задержкой 1→2→4 с, до 3 попыток; аналог логики в очереди агента
- [ ] **Батчинг запросов в embeddings.ts** — объединять конкурентные вызовы в `batchEmbeddings(texts[], limit=4)`; воркер уже есть, нет группировки входящих запросов
- [ ] **Провайдер Groq** — совместим с OpenAI API; переиспользовать `openaiProvider` с другим `baseUrl`; отдельный пункт в UI
- [ ] **Провайдер Together AI** — аналогично Groq; отдельный пункт в UI
- [ ] **Инструмент `trigger_github_workflow`** — `gh workflow run {workflow_id}`; просмотр статуса последних runs; GitHub token уже есть в настройках
- [ ] **MemoryStorage interface для memory.ts** — изолировать `fs.readFile`/`fs.writeFile` за интерфейсом; в тестах подменяется in-memory реализацией

---

### 🟠 Сложные — новый модуль или архитектурное изменение, 1–3 дня

- [ ] **Очередь сообщений per-chat** — `enqueuedMessages` вынести в Map по chatId; агенты в разных чатах не блокируют друг друга
- [ ] **Каждый чат хранит свой projectPath** — агент получает путь из чата, не из глобального состояния; UI переключает проект без перезагрузки
- [ ] **Интеграция с Jira** — REST API: `POST /rest/api/3/issue`; Jira URL и API token в настройках (новая вкладка)
- [ ] **Интеграция с Linear** — GraphQL API: мутация `issueCreate`; API key в настройках
- [ ] **CI → Release pipeline** — GitHub Actions: при push тега `v*` → `electron-builder --publish always` → GitHub Releases; артефакты: `.exe`, `.dmg`, `.AppImage`
- [ ] **`electron-updater`** — проверяет GitHub Releases при старте; скачивает в фоне; баннер «Обновление готово» с кнопкой перезапуска
- [ ] **Кроссплатформенность** — `.sh` launcher для Linux/macOS; CI-матрица `windows-latest` + `ubuntu-latest` + `macos-latest`; условные пути в main process
- [ ] **Docker-образ для разработки** — `Dockerfile` с Node.js + Ollama; `docker-compose.yml` с hot reload
- [ ] **Виртуализация сообщений в чате** — внедрить `@tanstack/react-virtual` для основного списка в `ChatPanel`; виртуализация сейчас только для истории и памяти
- [ ] **Отключение инструментов per-project** — чекбоксы по группам инструментов в настройках проекта; отключённые не включаются в список tools при запросе
- [ ] **Базовый класс StreamingChatProvider** — вынести общую логику стриминга из `ollamaProvider.ts` и `openaiProvider.ts`; конкретные провайдеры реализуют только `buildRequest()` и `parseChunk()`
- [ ] **Circuit breaker для API-провайдеров** — closed → open (5 подряд ошибок) → half-open (через 30 с); в состоянии open запросы отклоняются немедленно; статус виден в топбаре
- [ ] **Интеграционные тесты провайдеров с mock-сервером** — `nock` или `msw`; тесты `ollamaProvider` и `openaiProvider` без сети; сценарии: tool call, stream, 429, разрыв соединения
- [ ] **Семантическая верификация в actionVerification.ts** — дополнить regexp-проверку вызовом модели для граничных случаев; снизить число ложных блокировок
- [ ] **Декомпозиция AgentRunner** — `agent.ts` ≈1300 строк; вынести `ContextManager`, `ToolExecutor`, `SelfImprovementOrchestrator`, `LoopGuard`, `ResponseEmitter`; каждый класс ≤300 строк

---

### 🔴 Масштабные — новая инфраструктура, неделя и более

- [ ] **Qdrant: настройки подключения** — вкладка в настройках: URL + API key; проверка соединения; `qdrant-client` в зависимостях
- [ ] **Индексирование проекта в Qdrant** — инструмент `index_project`: чанкует файлы, получает эмбеддинги, пишет в коллекцию; индикатор прогресса
- [ ] **Инструмент `search_knowledge_base`** — семантический поиск по проиндексированному проекту; возвращает top-N чанков с путями
- [ ] **Поддержка Milvus** — альтернативный адаптер; выбор провайдера базы знаний в настройках
- [ ] **Парсинг схем MCP-серверов** — читать `/.well-known/mcp` или JSON-схему по URL; преобразовывать `tools[]` в формат `AgentTool`
- [ ] **Регистрация MCP-инструментов в агенте** — динамически добавлять инструменты из MCP в список tools перед каждым прогоном
- [ ] **UI добавления MCP-серверов** — вкладка в настройках: список серверов, кнопка «+ Добавить по URL», статус подключения
- [ ] **Передача результатов обратно в MCP** — `POST /mcp/tool_result` после выполнения; поддержка stateful-сессий
- [ ] **Загрузка плагинов из `~/.codeviper/plugins/`** — сканировать `*.ts`/`*.js` при старте; читать `export default { name, description, tools[] }`
- [ ] **Компиляция плагинов через esbuild** — бандлить в CommonJS в main process; кэшировать скомпилированный результат
- [ ] **Изолированное выполнение плагинов** — запускать в `worker_thread` с ограниченным API (`fs` только в projectPath, нет `net`)
- [ ] **UI управления плагинами** — вкладка в настройках: список, включить/выключить, инструменты плагина, кнопка «Открыть папку плагинов»
- [ ] **NSIS-установщик** — `electron-builder` + NSIS-скрипт; клонирует репозиторий в `%APPDATA%\CodeViper\source\`; ярлык на рабочем столе
- [ ] **Portable Node.js в комплекте** — скачивать Node.js в `resources/node/` при сборке; использовать для `npm run build` при самопересборке агента
- [ ] **Движок `node-llama-cpp` в Electron** — установить и скомпилировать нативный модуль под win32/linux/darwin; smoke-тест: загрузить GGUF, получить токен, выгрузить
- [ ] **Загрузчик модели оркестратора** — при первом старте скачивать `Qwen2.5-1.5B-Instruct.Q4_K_M.gguf` (~900 МБ) в `userData/orchestrator/`; прогресс-бар в UI; кнопка «Скачать» в настройках
- [ ] **Класс `OrchestratorModel`** — singleton в main process; `analyze(msg): { plan, rephrased, isComplex }`; без стриминга, только итог
- [ ] **Интеграция оркестратора в `AgentRunner`** — перед прогоном вызывать `orchestrator.analyze()`; план добавляется в системный промпт; если `isComplex=true` — использовать перефразированный запрос
- [ ] **UI: индикатор «Планирую...»** — stream-событие `orchestrating`; серый чип в чате; после — скрывать или показывать раскрывающийся блок с планом
- [ ] **Настройки оркестратора** — тумблер «Включить оркестратор»; порог `minMessageLength` (80 символов); кнопка «Удалить модель»
- [ ] **Сигнальный сервер P2P** — VPS с реестром узлов (ID, GPU/RAM, нагрузка); Node.js + WebSocket + Redis; REST API для регистрации и опроса
- [ ] **Аккаунтная система P2P** — регистрация по email/GitHub OAuth; JWT-токен определяет репутацию и лимиты
- [ ] **Лимит ресурсов узла P2P** — `systeminformation`; при отдаче не более 20% GPU и 15% CPU; при превышении — пауза новых задач
- [ ] **Режим «Поделиться мощностью»** — переключатель в настройках; узел регистрируется на сервере; принимает входящие задачи
- [ ] **Очередь входящих задач P2P** — не более 3 одновременно; приоритет по балансу кредитов; таймаут 60 с на задачу
- [ ] **Маршрутизация запросов P2P** — сервер ищет свободный узел с нужной моделью; если нет — откат на локальную Ollama
- [ ] **Шифрование трафика P2P** — TLS между узлами; end-to-end шифрование промптов; владелец узла не видит чужих данных
- [ ] **Система репутации P2P** — кредиты за отданные вычисления; трата при использовании чужих; история в UI
- [ ] **Экран согласия P2P** — явный диалог при первом включении с объяснением что именно будет передаваться

---

## ✅ Сделано
- Провайдер Claude (Anthropic API): streamed responses, tool use, JSON schema validation через `@anthropic-ai/sdk`
- Провайдер Gemini: REST SSE стриминг (`streamGenerateContent`), thinking, `tool_config` (AUTO/ANY), убран SDK
- TRON (Token Reduced Object Notation): парсер и сериализатор для сжатия данных (~27% экономия); применён в localStorage (история команд), логировании NDJSON (agent logs), IPC коммуникации

**UI**
- Кнопка «Сжать историю» в превью контекста: при заполнении > 60% появляется в поповере; вызывает принудительную суммаризацию через IPC `summarize-context`
- Убран заголовок активного чата из топбара (показывал внутренние задачи самоулучшения); иконка приложения из `resources/icon.png`; логотип PNG в топбаре вместо эмодзи
- Раздельные API-ключи для DeepSeek / OpenAI / OpenRouter (шифрование `safeStorage`); миграция со старого единого `providerApiKey`
- Список моделей OpenRouter с фильтром tool calling, поиском и отображением размера контекста
- Раздельные списки чатов для вкладок Chat и Code (`SavedChat.mode`); кнопки чата скрыты по умолчанию, появляются при наведении

**Производительность**
- Динамический системный промпт: в режиме Chat — только базовый промпт (~200 токенов), без инструментов, дерева проекта и памяти; экономия 10–20% токенов на запрос
- Вкладка «Производительность» в настройках: тумблеры «Режим энергосбережения» (батчинг 300 мс, без анимаций), «Отключить CPU/GPU-статы», «Обновлять PR только вручную»
- Интервал опроса CPU/GPU снижен с 1 с до 3 с (`systemStats.ts`)
- Устойчивость при смене монитора: флаги `--disable-gpu-process-crash-limit` + `--in-process-gpu`; авто-reload рендерера при крашах GPU/renderer → `CrashRecoveryDialog` восстанавливает сессию

**Ядро агента**
- Очистка editSnapshots при старте каждого прогона — предотвращает утечку памяти в длинных сессиях
- Стриминг, кнопка «Стоп», цикл `while(true)`, инструменты (create/edit/append/delete/move/grep/find/git), tool choice, парсинг text tool call, детектор опасных задач, защита `parseToolArgs`
- Рефакторинг `agent.ts` → 6 модулей; параллельное выполнение инструментов (Promise.all) при cloud API; удалены жёсткие лимиты шагов/прогонов
- Агент не молчит при пустом ответе; не останавливается на «намерении»; пропускает tool call для информационных вопросов
- Добавлены инструменты для GitHub, файловых операций и кратких сводок по проекту

**Провайдеры и модели**
- Dual-provider режим (Ollama + cloud одновременно); нативный tool calling OpenAI/DeepSeek; `max_tokens`, `temperature` для cloud; работа без Ollama при cloud-провайдере
- Провайдер OpenRouter (агрегатор: GPT-4o, Claude, Gemini, Llama и др.) — основной и облачный
- Выбор модели в топбаре; динамический список моделей DeepSeek; совместимость моделей (✓/⚠ по RAM); управление моделями Ollama (каталог, автовыбор, скачать/удалить)
- Статистика «Xс · Yk токенов» под последним сообщением агента; пульсирующая индикаторная полоса вместо бегущей; убрана нагрузка CPU/GPU из статусбара; оптимизация промпта и описаний инструментов

- Кэш контекст-превью в `useContextPreview`: IPC-запрос пропускается, если `{messagesKey, model}` не изменились с прошлого вызова

**Контекст и память**
- Настраиваемый порог суммаризации (50–85%, дефолт 85%) и тумблер «Агрессивное сжатие» (65%) в настройках вкладки Модель; слайдер заблокирован при включённом агрессивном сжатии
- Исключение reasoning (<think>...</think>) из истории контекста: опция в настройках (вкладка Модель), экономия 20–50% токенов для think-моделей
- Суммаризация при ~85% лимита; адаптивные лимиты (`computeAdaptiveLimits`); чип «Инструмент: Xk | История: N»
- Самообучение: `remember`, рефлексия, план самоулучшения, anti-loop, эмбеддинги (LRU 500, worker)
- Фикс гонки инициализации в `embeddingQueue.ts`: буферизация запросов до `ready`-события воркера, очистка очереди при падении воркера; 9 vitest-тестов
- Корректный контекст для облачных моделей: `getModelContextLimitTokens` распознаёт deepseek-*, gpt-*, claude-*, gemini-*; `modelContextLength` из API сохраняется в настройках и прокидывается до `computeContextUsage`
- Оптимизация токенов: инструменты разбиты на 9 категорий (file, git, github, memory, skills, todo, package, codeviper, ollama) с кэшированием преобразованных схем; `getAgentTools(selfImproveMode)` исключает 19 инструментов в обычном режиме — экономия ~35% tools JSON на каждый запрос, ~60% в обычном режиме; `buildSelfEditContext()` только в self-improve
- Инструменты GitHub: `create_issue` (title, body, labels), `create_pr`, `list_issues`, `open_issue`, `trigger_github_workflow` — через `gh` CLI
- Провайдер Gemini: прямой REST (`streamGenerateContent?alt=sse`), thinking chunks, `tool_config`, id в function calls/responses
- Новый инструмент `search_in_file`: поиск текста в одном конкретном файле без ограничения по размеру (включает файлы >512KB)
- `grep_files` теперь явно сообщает о пропущенных файлах >512KB и предлагает использовать `search_in_file`
- Скилл Todo List для агента: инструменты `set_todo_list`, `complete_todo_item`, `clear_todo_list`; компактная панель `TodoPanel` прикреплена над полем ввода, обновляется через stream-событие `todo_update`
- Черновики при обрыве стрима: `interruptedDraft`, баннер «Повторить»
- RAG (Retrieval-Augmented Generation) для контекста: `contextRAG.ts` индексирует сообщения чата в векторной БД с эмбеддингами; при построении контекста вместо просто последних N сообщений ищет релевантные по семантике — экономия токенов на длинных диалогах и сохранение важной информации

**UI/UX**
- История чатов: папки, поиск, drag-and-drop, pin, теги, привязка к проекту; экспорт/импорт JSON+Markdown
- Вложения: файлы, drag-and-drop, Ctrl+V скриншоты, base64 для мультимодальных, лимит 10 файлов/200 КБ
- Группировка одинаковых вызовов инструментов в чате; открытие файлов из текста агента
- Подсветка кода, ANSI-цвета, анимации, горячие клавиши, звуковые уведомления, тёмная тема

**Архитектура фронтенда**
- `AgentContext` (`useReducer` + Context): phase, runStats, метрики — без пропсов
- `ChatContext`: messages, activeChatId, chatStore, activeChat, interruptedDraft — без пропсов
- `QueueContext`: chatBusy — без пропсов; единый `resetQueue()` сбрасывает и очередь, и стрим

**Производительность**
- Workers: grep/find, эмбеддинги, парсинг больших файлов — main не блокируется
- Батчинг IPC token/thinking; батчинг токенов в UI (150 мс flush); дебаунс контекст-превью 1000 мс; `setInterval` runStats снижен
- `React.memo` на `MessageBody`; виртуализация длинных списков (`@tanstack/react-virtual`); ленивая загрузка (`React.lazy`)
- Защита от зацикливания: подсказка модели + продолжение прогона без остановки (лимиты шагов/попыток удалены)

**Git и проекты**
- Параллельные агенты per-chat (`Map<chatId, ...>`); git-sync (stash/rebase/ff-only)
- Diff перед правками (`preview_edit`): LCS → unified diff → UI с «Применить»/«Отмена»; `AbortSignal` в `createUnifiedDiff()` — проверка каждые 500 итераций LCS, fallback на построчный diff при отмене
- История изменений файлов (NDJSON-лог, `show_file_history`); ветки и PR агента
- Отложенное применение правок вне режима самоулучшения (git stash); автовосстановление после краша

**Безопасность и инфраструктура**
- `run_command` без `shell: true`; `assertInsideProject`; blocklist; шифрование API-ключей (`safeStorage`)
- ESLint + Prettier + lint-staged + husky; vitest 37+ тестов; E2E Playwright+Electron; нагрузочные тесты
- Семантическое версионирование; GitHub Actions CI; branch protection; доступность (WCAG AA)
- Статус PR в UI (CI-статус); `PrStatusPanel` опрашивает только при открытой панели (`isOpen`), интервал 300 с; автодополнение в терминале; метрики (tok/s, NDJSON-лог)
- Zod-схема `PersistedSettingsSchema` в `settings.ts`: тип `PersistedSettings` выведен через `z.infer<>`; `safeParse()` при загрузке с детальным логом ошибок и fallback на `normalize()`
