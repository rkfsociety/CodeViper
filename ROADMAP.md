# Дорожная карта CodeViper

Планы развития и список выполненного. Назад в [README](README.md).

## 📋 В планах

Задачи отсортированы по сложности выполнения агентом: от однофайловых правок до отдельных инфраструктурных подсистем. Нумерация сквозная, начинается с 1.

---

### 🟢 Простые — 1 файл, 1–2 часа

- [ ] **1. Очистка editSnapshots при старте прогона** — в `agentHandlersProject.ts` сбрасывать Map `editSnapshots` при каждом новом `runId`; предотвращает утечку памяти в длинных сессиях
- [ ] **2. Фикс гонки инициализации в embeddingQueue.ts** — запросы, пришедшие до `online`-события воркера, класть в очередь и отправлять сразу после готовности listener'а; сейчас они теряются

---

### 🟡 Средние — несколько файлов, полдня–день

- [ ] **3. PR-панель: опрос только при открытой панели** — `PrStatusPanel` принимает `isOpen`; `setInterval` запускается только при `isOpen === true`; интервал с 60 с до 300 с
- [ ] **4. Кэш контекст-превью по хешу** — в `useContextPreview` хранить последний результат для пары `{messagesLength, model}`; пропускать IPC-запрос, если вход не изменился
- [ ] **5. AbortSignal-таймаут в diffUtil.ts** — передавать `AbortSignal` в `createUnifiedDiff()`; LCS-алгоритм проверяет `signal.aborted` каждые N итераций; fallback на построчный diff; предотвращает зависание на файлах >5000 строк
- [ ] **6. Zod-схема для AgentSettings** — валидация при загрузке конфига через `z.parse()`; понятные сообщения об ошибках вместо runtime-падений; TypeScript-тип генерируется из схемы
- [ ] **7. Retry + backoff в selfCommit.ts** — обернуть git-операции в retry с задержкой 1→2→4 с, до 3 попыток; аналог логики в очереди агента
- [ ] **8. Батчинг запросов в embeddings.ts** — объединять конкурентные вызовы в `batchEmbeddings(texts[], limit=4)`; воркер уже есть, нет группировки входящих запросов
- [ ] **9. LRU-кэш grep_files в fileSearchInWorker.ts** — кэш по ключу `{pattern, dir, glob}` с инвалидацией по `mtime`; аналог LRU 500 записей для эмбеддингов
- [ ] **10. Настраиваемый порог суммаризации** — `CONTEXT_SUMMARIZE_THRESHOLD` в AgentSettings (50–85%, дефолт 85%); тумблер «Агрессивное сжатие» в настройках (65%); экономия 30–40% на длинных диалогах
- [ ] **11. Кнопка «Сжать историю» в превью контекста** — при заполнении > 60% показывать рекомендацию в `AgentContextBar`; кнопка запускает принудительную суммаризацию через IPC-вызов `summarize-context`
- [ ] **12. Удаление старых вызовов инструментов из контекста** — оставлять результаты только последних K вызовов (дефолт 5); более старые схлопываются в `[результат обрезан]`; экономия 10–15%
- [ ] **13. Автообрезка вложенных файлов** — файл > N КБ (дефолт 20 КБ): первые + последние 50 строк с маркером `... (X строк обрезано) ...`
- [ ] **14. Провайдер Groq** — совместим с OpenAI API; переиспользовать `openaiProvider` с другим `baseUrl`; отдельный пункт в UI
- [ ] **15. Провайдер Together AI** — аналогично п. 15; отдельный пункт в UI
- [ ] **16. Инструмент `trigger_github_workflow`** — `gh workflow run {workflow_id}`; просмотр статуса последних runs; GitHub token уже есть в настройках
- [ ] **17. Инструмент `create_issue`** — создание задач в GitHub Issues через `gh issue create`; поля: title, body, labels
- [ ] **18. UI: индикатор работы в других чатах** — иконка/пульс рядом с чатом в списке, если в нём сейчас работает агент
- [ ] **19. MemoryStorage interface для memory.ts** — изолировать `fs.readFile`/`fs.writeFile` за интерфейсом; в тестах подменяется in-memory реализацией
- [ ] **20. Поддержка альтернативных моделей оркестратора** — кнопка «Выбрать свою модель» → `dialog.showOpenDialog` с фильтром `.gguf`; путь сохраняется в `config.json`

---

### 🟠 Сложные — новый модуль или архитектурное изменение, 1–3 дня

- [ ] **21. Провайдер Claude (Anthropic SDK)** — установить `@anthropic-ai/sdk`; адаптер `claudeProvider.ts` по образцу `openaiProvider.ts`; UI: пункт «Claude» + поле API key
- [ ] **22. Провайдер Gemini** — `@google/generative-ai`; адаптер `geminiProvider.ts`; маппинг tool calling в формат Gemini
- [ ] **23. Очередь сообщений per-chat** — `enqueuedMessages` вынести в Map по chatId; агенты в разных чатах не блокируют друг друга
- [ ] **24. Каждый чат хранит свой projectPath** — агент получает путь из чата, не из глобального состояния; UI переключает проект без перезагрузки
- [ ] **25. Интеграция с Jira** — REST API: `POST /rest/api/3/issue`; Jira URL и API token в настройках (новая вкладка)
- [ ] **26. Интеграция с Linear** — GraphQL API: мутация `issueCreate`; API key в настройках
- [ ] **27. CI → Release pipeline** — GitHub Actions: при push тега `v*` → `electron-builder --publish always` → GitHub Releases; артефакты: `.exe`, `.dmg`, `.AppImage`
- [ ] **28. `electron-updater`** — проверяет GitHub Releases при старте; скачивает в фоне; баннер «Обновление готово» с кнопкой перезапуска
- [ ] **29. Кроссплатформенность** — `.sh` launcher для Linux/macOS; CI-матрица `windows-latest` + `ubuntu-latest` + `macos-latest`; условные пути в main process
- [ ] **30. Docker-образ для разработки** — `Dockerfile` с Node.js + Ollama; `docker-compose.yml` с hot reload
- [ ] **31. Виртуализация сообщений в чате** — внедрить `@tanstack/react-virtual` для основного списка в `ChatPanel`; виртуализация сейчас только для истории и памяти
- [ ] **32. Секция «Производительность» в настройках** — новая вкладка: тумблер «Режим энергосбережения» (батчинг 300 мс, без анимаций), тумблер «Отключить CPU/GPU-статы», тумблер «Обновлять PR только вручную»
- [ ] **33. Динамический системный промпт** — разбить `BASE_SYSTEM_PROMPT` на базовый (~200 токенов) + модули (инструменты, безопасность, Git); в режиме Chat — только базовый; экономия 10–20% на запрос
- [ ] **34. Отключение инструментов per-project** — чекбоксы по группам инструментов в настройках проекта; отключённые не включаются в список tools при запросе
- [ ] **35. Базовый класс StreamingChatProvider** — вынести общую логику стриминга из `ollamaProvider.ts` и `openaiProvider.ts`; конкретные провайдеры реализуют только `buildRequest()` и `parseChunk()`
- [ ] **36. Circuit breaker для API-провайдеров** — closed → open (5 подряд ошибок) → half-open (через 30 с); в состоянии open запросы отклоняются немедленно; статус виден в топбаре
- [ ] **37. Интеграционные тесты провайдеров с mock-сервером** — `nock` или `msw`; тесты `ollamaProvider` и `openaiProvider` без сети; сценарии: tool call, stream, 429, разрыв соединения
- [ ] **38. Семантическая верификация в actionVerification.ts** — дополнить regexp-проверку вызовом модели для граничных случаев; снизить число ложных блокировок
- [ ] **39. Декомпозиция AgentRunner** — `agent.ts` ≈1300 строк; вынести `ContextManager`, `ToolExecutor`, `SelfImprovementOrchestrator`, `LoopGuard`, `ResponseEmitter`; каждый класс ≤300 строк

---

### 🔴 Масштабные — новая инфраструктура, неделя и более

- [ ] **40. Qdrant: настройки подключения** — вкладка в настройках: URL + API key; проверка соединения; `qdrant-client` в зависимостях
- [ ] **41. Индексирование проекта в Qdrant** — инструмент `index_project`: чанкует файлы, получает эмбеддинги, пишет в коллекцию; индикатор прогресса
- [ ] **42. Инструмент `search_knowledge_base`** — семантический поиск по проиндексированному проекту; возвращает top-N чанков с путями
- [ ] **43. Поддержка Milvus** — альтернативный адаптер; выбор провайдера базы знаний в настройках
- [ ] **44. Парсинг схем MCP-серверов** — читать `/.well-known/mcp` или JSON-схему по URL; преобразовывать `tools[]` в формат `AgentTool`
- [ ] **45. Регистрация MCP-инструментов в агенте** — динамически добавлять инструменты из MCP в список tools перед каждым прогоном
- [ ] **46. UI добавления MCP-серверов** — вкладка в настройках: список серверов, кнопка «+ Добавить по URL», статус подключения
- [ ] **47. Передача результатов обратно в MCP** — `POST /mcp/tool_result` после выполнения; поддержка stateful-сессий
- [ ] **48. Загрузка плагинов из `~/.codeviper/plugins/`** — сканировать `*.ts`/`*.js` при старте; читать `export default { name, description, tools[] }`
- [ ] **49. Компиляция плагинов через esbuild** — бандлить в CommonJS в main process; кэшировать скомпилированный результат
- [ ] **50. Изолированное выполнение плагинов** — запускать в `worker_thread` с ограниченным API (`fs` только в projectPath, нет `net`)
- [ ] **51. UI управления плагинами** — вкладка в настройках: список, включить/выключить, инструменты плагина, кнопка «Открыть папку плагинов»
- [ ] **52. NSIS-установщик** — `electron-builder` + NSIS-скрипт; клонирует репозиторий в `%APPDATA%\CodeViper\source\`; ярлык на рабочем столе
- [ ] **53. Portable Node.js в комплекте** — скачивать Node.js в `resources/node/` при сборке; использовать для `npm run build` при самопересборке агента
- [ ] **54. Движок `node-llama-cpp` в Electron** — установить и скомпилировать нативный модуль под win32/linux/darwin; smoke-тест: загрузить GGUF, получить токен, выгрузить
- [ ] **55. Загрузчик модели оркестратора** — при первом старте скачивать `Qwen2.5-1.5B-Instruct.Q4_K_M.gguf` (~900 МБ) в `userData/orchestrator/`; прогресс-бар в UI; кнопка «Скачать» в настройках
- [ ] **56. Класс `OrchestratorModel`** — singleton в main process; `analyze(msg): { plan, rephrased, isComplex }`; без стриминга, только итог
- [ ] **57. Интеграция оркестратора в `AgentRunner`** — перед прогоном вызывать `orchestrator.analyze()`; план добавляется в системный промпт; если `isComplex=true` — использовать перефразированный запрос
- [ ] **58. UI: индикатор «Планирую...»** — stream-событие `orchestrating`; серый чип в чате; после — скрывать или показывать раскрывающийся блок с планом
- [ ] **59. Настройки оркестратора** — тумблер «Включить оркестратор»; порог `minMessageLength` (80 символов); кнопка «Удалить модель»
- [ ] **60. Сигнальный сервер P2P** — VPS с реестром узлов (ID, GPU/RAM, нагрузка); Node.js + WebSocket + Redis; REST API для регистрации и опроса
- [ ] **61. Аккаунтная система P2P** — регистрация по email/GitHub OAuth; JWT-токен определяет репутацию и лимиты
- [ ] **62. Лимит ресурсов узла P2P** — `systeminformation`; при отдаче не более 20% GPU и 15% CPU; при превышении — пауза новых задач
- [ ] **63. Режим «Поделиться мощностью»** — переключатель в настройках; узел регистрируется на сервере; принимает входящие задачи
- [ ] **64. Очередь входящих задач P2P** — не более 3 одновременно; приоритет по балансу кредитов; таймаут 60 с на задачу
- [ ] **65. Маршрутизация запросов P2P** — сервер ищет свободный узел с нужной моделью; если нет — откат на локальную Ollama
- [ ] **66. Шифрование трафика P2P** — TLS между узлами; end-to-end шифрование промптов; владелец узла не видит чужих данных
- [ ] **67. Система репутации P2P** — кредиты за отданные вычисления; трата при использовании чужих; история в UI
- [ ] **68. Экран согласия P2P** — явный диалог при первом включении с объяснением что именно будет передаваться

---

## ✅ Сделано

**UI**
- Убран заголовок активного чата из топбара (показывал внутренние задачи самоулучшения); иконка приложения из `resources/icon.png`; логотип PNG в топбаре вместо эмодзи
- Раздельные API-ключи для DeepSeek / OpenAI / OpenRouter (шифрование `safeStorage`); миграция со старого единого `providerApiKey`
- Список моделей OpenRouter с фильтром tool calling, поиском и отображением размера контекста

**Производительность**
- Интервал опроса CPU/GPU снижен с 1 с до 3 с (`systemStats.ts`)
- Устойчивость при смене монитора: флаги `--disable-gpu-process-crash-limit` + `--in-process-gpu`; авто-reload рендерера при крашах GPU/renderer → `CrashRecoveryDialog` восстанавливает сессию

**Ядро агента**
- Стриминг, кнопка «Стоп», цикл `while(true)`, инструменты (create/edit/append/delete/move/grep/find/git), tool choice, парсинг text tool call, детектор опасных задач, защита `parseToolArgs`
- Рефакторинг `agent.ts` → 6 модулей; параллельное выполнение инструментов (Promise.all) при cloud API; удалены жёсткие лимиты шагов/прогонов
- Агент не молчит при пустом ответе; не останавливается на «намерении»; пропускает tool call для информационных вопросов

**Провайдеры и модели**
- Dual-provider режим (Ollama + cloud одновременно); нативный tool calling OpenAI/DeepSeek; `max_tokens`, `temperature` для cloud; работа без Ollama при cloud-провайдере
- Провайдер OpenRouter (агрегатор: GPT-4o, Claude, Gemini, Llama и др.) — основной и облачный
- Выбор модели в топбаре; динамический список моделей DeepSeek; совместимость моделей (✓/⚠ по RAM); управление моделями Ollama (каталог, автовыбор, скачать/удалить)
- Чип «Xс · Yk tok» в топбаре; оптимизация промпта и описаний инструментов

**Контекст и память**
- Исключение reasoning (<think>...</think>) из истории контекста: опция в настройках (вкладка Модель), экономия 20–50% токенов для think-моделей
- Суммаризация при ~85% лимита; адаптивные лимиты (`computeAdaptiveLimits`); чип «Инструмент: Xk | История: N»
- Самообучение: `remember`, рефлексия, план самоулучшения, anti-loop, эмбеддинги (LRU 500, worker)
- Черновики при обрыве стрима: `interruptedDraft`, баннер «Повторить»

**UI/UX**
- История чатов: папки, поиск, drag-and-drop, pin, теги, привязка к проекту; экспорт/импорт JSON+Markdown
- Вложения: файлы, drag-and-drop, Ctrl+V скриншоты, base64 для мультимодальных, лимит 10 файлов/200 КБ
- Группировка одинаковых вызовов инструментов в чате; открытие файлов из текста агента
- Подсветка кода, ANSI-цвета, анимации, горячие клавиши, звуковые уведомления, тёмная тема

**Архитектура фронтенда**
- `AgentContext` (`useReducer` + Context): phase, runStats, метрики — без пропсов
- `ChatContext`: messages, activeChatId, chatStore, activeChat, interruptedDraft — без пропсов
- `QueueContext`: chatBusy — без пропсов; единый `resetQueue()` сбрасывает и очередь, и стрим

**Производительность**
- Workers: grep/find, эмбеддинги, парсинг больших файлов — main не блокируется
- Батчинг IPC token/thinking; батчинг токенов в UI (150 мс flush); дебаунс контекст-превью 1000 мс; `setInterval` runStats снижен
- `React.memo` на `MessageBody`; виртуализация длинных списков (`@tanstack/react-virtual`); ленивая загрузка (`React.lazy`)
- Восстановление после циклов агента: подсказка модели + продолжение прогона вместо остановки (до 3 попыток)

**Git и проекты**
- Параллельные агенты per-chat (`Map<chatId, ...>`); git-sync (stash/rebase/ff-only)
- Diff перед правками (`preview_edit`): LCS → unified diff → UI с «Применить»/«Отмена»
- История изменений файлов (NDJSON-лог, `show_file_history`); ветки и PR агента
- Отложенное применение правок вне режима самоулучшения (git stash); автовосстановление после краша

**Безопасность и инфраструктура**
- `run_command` без `shell: true`; `assertInsideProject`; blocklist; шифрование API-ключей (`safeStorage`)
- ESLint + Prettier + lint-staged + husky; vitest 37+ тестов; E2E Playwright+Electron; нагрузочные тесты
- Семантическое версионирование; GitHub Actions CI; branch protection; доступность (WCAG AA)
- Статус PR в UI (CI-статус, опрос 60 с); автодополнение в терминале; метрики (tok/s, NDJSON-лог)
